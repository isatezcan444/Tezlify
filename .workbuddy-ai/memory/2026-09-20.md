# 2026-09-20 — Work log

_(continuation of the 2026-09-19 WhatsApp session; clock rolled past midnight)_

## Production drift audit — Oracle `/opt/tezlify` (READ-ONLY, no mutating command executed)

Produced `WHATSAPP_PRODUCTION_DRIFT_AUDIT.md` at repo root.

### Corrections to the brief (both verified against the live box)
- **"13 modified files on Oracle" is wrong.** Production's tracked tree is **clean**
  (`git status --porcelain --untracked-files=no` empty; `git diff` empty; `git diff --cached` empty).
  `git status --porcelain` returns exactly 13 lines, **all `??` untracked**. They are release dirs,
  symlinks, backups and debug scripts — not source drift. ⇒ no uncommitted production work can be lost,
  and no merge conflict is possible.
- **Local HEAD is `6f4aa00`, not `91dbfe6`.** `91dbfe6` is its parent (commit added after the snapshot).
  Drift distance is still 18.
- **Oracle does not have the objects `2d13064` / `5f8d25d` at all** — never fetched. Last
  `.git/FETCH_HEAD` write 2026-09-18 13:51; images built 13:52.

### New facts discovered this session
- **Deploy topology: the frontend is NOT served from the source tree.** Caddy bind-mounts
  `/opt/tezlify/frontend_candidate:/srv/frontend:ro`, and `frontend_candidate` is a **symlink** to
  `releases/v20260918_phone_locale_and_baileys_lid_fix`. Docker resolves the symlink **at container
  creation** (evidence: release dir built 12:51, caddy started 12:54:12) ⇒ **swapping the symlink alone
  changes nothing; caddy must be `--force-recreate`d.**
- **Production has no `frontend/node_modules`** ⇒ the frontend is built off-box and uploaded as a
  release dir. `frontend/dist` on the box is stale (Sep 15) and unused.
- **`.staging_db_secret` (49 B, mode 600) is NOT gitignored** — one `git add -A` from being committed.
  Fix must go in `.git/info/exclude`, **not** `.gitignore`, because `.gitignore` is itself modified by
  `4fa1882` and a local edit would block the fast-forward.
- **`frontend/.env.production` IS tracked** in git (un-ignored by `!frontend/.env.production`).
- **A live session will be interrupted by the deploy:** `whatsapp_sessions.id=68`, user
  `e512dd40-…`, `status=CONNECTED`; `socket_leases` holds 1 row (`a618f89c-…`, gen=6);
  `WHATSAPP_AUTO_RESTORE="false"` ⇒ no auto-relink after gateway recreate.
- **Disk/RAM headroom is fine:** `/` 86 GB free (11% used), 21 GB RAM available.

### Confirmed production DB baselines (read-only SELECTs)
```
messages                 = 1332 rows   dup_groups = 0   ← new unique index WILL be created
uq_msg_conv_wa_message_id = ABSENT
whatsapp_sessions        = 3           gateway_sessions = 31
lid_mappings             = 3139 rows / 12 distinct sessions
socket_leases            = 1 row (a618f89c, gen=6)
FK: socket_leases.session_id → whatsapp_private.gateway_sessions(session_id)  ✔ present
```

### The 2741-file flood input, confirmed on the live box
23 session dirs; **2 741 `lid-mapping-*.json` files, ALL in `a618f89c-8901-456f-816c-92e42dbf9a67`**
(the currently connected session); the other 22 dirs hold 0 each. Post-`2d13064` a *new* ephemeral
pairing scans only its own dir → 0 files / 0 INSERTs. Note: re-syncing the *connected* session still
legitimately reads its own 2 741 files — the flood test must use a fresh ephemeral pairing.

### Deploy-safety conclusions
- `f6ec68d` is an ancestor of `origin/main`; prod has 0 commits of its own; tree clean ⇒
  **`git merge --ff-only origin/main` cannot conflict.** 18 commits / 93 files.
- **Zero overlap** between the 93 incoming paths and the 13 untracked paths.
- Migration `ensure_messages_wa_message_id_unique` runs automatically in the backend `lifespan`,
  is idempotent, non-destructive, and self-blocks (logs `BLOCKED`, creates nothing) on duplicates.
  Pre-flight returned `dup_groups = 0` ⇒ index will be created.
- Flagged but non-blocking: `6f4aa00` commits `frontend/vite.config.local.ts`, a local-only Vite config,
  into `main`.

### Still open (blocking a deploy decision)
Maintenance window for the live session drop · frontend build/upload approval + `VITE_API_URL` check ·
full-FF vs subset scope · `.git/info/exclude` approval · real device for the E2E pairing test
(`LIVE DEVICE E2E = NOT RUN`).

---

## DEPLOY EXECUTED — `f6ec68d` → `6f4aa00` (2026-09-19 21:51 → 22:12 UTC)

Full report: `WHATSAPP_PRODUCTION_DEPLOY_REPORT.md`. **All 14 phases run; no STOP condition hit.**
Phases 12–13 (real phone, post-connect smoke) **NOT RUN — no physical device**.

**Artefacts now in production**
- git `6f4aa00df67ffc064592749b2ed47a89eec0e0e8`; working tree clean
- backend `sha256:5002fe89203bd02997a6ec51861070de0e008a751b19c6e66ce51f24dcad4bbc`
- gateway `sha256:1a44410c768d1eb6b19b9e7406f0978a7bbce05be20e96c815cbe8feeefc0cd1`
- frontend `releases/v20260919_phase6_5_lid_scope_and_lease` → `index-DPQvI-dK.js`
- rollback: `backups/predeploy_20260919_215146/` (db.dump 4.76 MB) + `tezlify-{backend,gateway}:backup-20260919_215146`

**BEFORE → AFTER (the two defects)**
- `POST /pairing/start` **~21.6 s → 0.207–0.222 s** (≈97×); first QR **0.610–0.635 s** p50 0.617
- LID files scanned for a new ephemeral session **2741 → 0**; doomed FK-23503 LID INSERTs **2741 → 0**
- ephemeral lease: **no acquire/renew, no lease-lost, alive at 70 s** (old code died ~14–30 s)
- persistent lease: **acquire + renewal every 15 s = TTL/3, generation stable** — verified directly
- migration `uq_msg_conv_wa_message_id` **CREATED** (dup_groups was 0)
- real Chrome: QR **208×208** non-zero DOM, **3 rotations in 45 s**, **WS 101**, **0 console errors**
- `lid_mappings` **3139 → 3139** (zero cross-tenant writes); protected 2741-file dir untouched

**Two mechanisms worth never forgetting**
1. **Caddy bind-mounts the `frontend_candidate` SYMLINK; Docker resolves it at container creation.**
   Swapping the symlink alone changes nothing — `--force-recreate caddy` is mandatory. Proven by
   before/after: caddy only started serving `index-DPQvI-dK.js` after the force-recreate.
2. **`WHATSAPP_AUTO_RESTORE=false`** ⇒ the gateway recreate dropped the live socket
   (`restored_sessions: 0`) and it was **not** restored. `socket_leases` 1 → 0. Session `id=68`
   (`e512dd40-…`) still reports a **stale `CONNECTED`** — **manual relink required**.

**Residue I created and deliberately did NOT delete** (production DB writes forbidden): +2
`gateway_sessions` rows (`f872268d-…`, `af59b362-…`), +7 `event_outbox` rows, +1 session dir
`37c4da78-…`. Cleanup SQL is in the report §9, pending approval.

**Non-regressions (verified pre-existing, do not chase)**
- `conversations` 449→447 and `contacts` 1848→1846: the pre-existing `purge_raw_jid_identity_data`,
  unchanged by the 18 commits and already wired in the old `main.py:116`.
- `event_outbox` FK-23503 (`event_outbox_session_id_fkey`) for No-Create ephemeral sessions — a
  *different* FK from the LID defect; present in the pre-deploy log too. Not a regression.

## Follow-up: closing the Phase 12/13 gap as far as a device-less run allows

Phases 12/13 need a physical phone → **`LIVE DEVICE E2E = NOT RUN`, unchanged.** What I *could* do was
assert the promotion/lease path (which production could never reach, since promotion fires on
`connection.update{connection:'open'}` from a real scan) against the **real** adapter and a **real** PG.

**Ran the gateway harness: 22/22 pass, 0 fail, 0 skip.** Note the old baseline was 21 pass + 1 skip —
`test-phase6-6-lease-real-pg.mjs` self-skips without `GATEWAY_LEASE_TEST_URL`; given one it runs, so this
is now 22 executed suites. The harness needs **no production DB** (`createFakeLeasePool()`), and the real-PG
variant uses **local** PostgreSQL (`127.0.0.1:5432`, `data_directory=/opt/homebrew/var/postgresql@17` —
verified, not a tunnel). It cleans up after itself: `socket_leases=0`, `gateway_sessions=0`, no stray dirs.

`test-phase6-6-lease-real-pg` (7 checks) confirms:
- the FK is real: `acquire` throws **23503**, `renew` is a **silent `false`** ← the defect's exact mechanism
- ephemeral pairing holds **no lease row** and arms **no timer**
- persistent session holds a real row, **renews it (`expires_at` advances)**
- ephemeral **survives one real renewal interval**
- **promoted pairing takes a real lease row and arms renewal** ← the branch production could not reach

**Frontend: 10/10 suites PASS** at `6f4aa00` (logic 43, dom 20, browser 7/7, pairing 8,
pairing-browser 12/12, merge-equivalence, chat-order 10, avatar, chat-scroll, message-merge).

**Backend: 1089 passed / 4 skipped** (rc=0) = the recorded baseline.

### Two traps hit here, both worth remembering

1. **`test-whatsapp-chat-order.mjs` had rotted — harness bug, not product.** It hand-transpiles
   `whatsappOrdering.ts` with `ts.transpileModule()` into a `data:` URL and strips imports by literal string
   match. When the module later gained `import { extractCleanPhone } from './whatsappIdentity';` the strip-list
   didn't know, so the `data:` URL threw `ERR_UNSUPPORTED_RESOLVE_REQUEST` — **a relative specifier can never
   resolve from `data:`**. Fixed by stripping that import too and inlining `whatsappIdentity.ts`.
   **Rule: a non-zero exit with a *resolution/type/transpile* error is the harness; with a failed `assert` it's
   the product.** Checked `rg "^import" <module>.ts` against the harness's strip-list. Lesson added to the
   `esbuild-frontend-verification` skill.

2. **The backend suite runs against SQLite `./tezlify.db`, NOT the local Postgres.** First run: 28 failures,
   all `whatsapp_sessions` owner-resolution tests with `bagli tenant sayisi=2`. `resolve_event_owner()`
   (repositories/sessions.py:132-145) selects **every** `CONNECTED` row with **no tenant filter** — correct,
   the event carries no `session_id` — and **fails closed** on >1 distinct owner. A leftover `CONNECTED` row
   for owner `67890123…` (id=2, `+905413749073`) plus each test's own seeded owner = 2 ⇒ ambiguous ⇒ skipped
   ⇒ assertion fails. **The product was behaving exactly as designed.** Re-run against a *copy* of the DB with
   the `CONNECTED` rows deleted (original never modified, verified) → **1089 passed / 4 skipped**.
   This is the "needs a session-free DB" precondition, now *demonstrated* rather than assumed.

## Post-deploy health check → a real production defect (read-only investigation)

Deploy confirmed **still healthy**: 4/4 containers up, HEAD `6f4aa00`, symlink → `releases/v20260919_phase6_5_lid_scope_and_lease`,
served `index-DPQvI-dK.js`, gateway `RestartCount=0`.

The health check then surfaced a **pre-existing defect cluster** (NOT a regression — the code changes did not
cause it). Full report: **`WHATSAPP_EPHEMERAL_SESSION_LEAK_DIAGNOSIS.md`**.

**F1 — a CANCELLED pairing keeps reconnecting forever, live right now.** Gateway `session_ref`
`cca3daa08c26` was created at epoch `1789855851` = **22:10:51 UTC**, byte-identical to `pairing/start` #1
(token `a7b1268c-…`). That pairing was **cancelled at 22:12:28 (200 OK)** — and the session was **still
reconnecting at 22:36:47, generation 10**, every close `408`, `reason: transient_disconnect`,
`auth_json_files: 0`, `registered: false`. 26 min of looping, 24 min of it after the cancel.
`invalidate()` (`src/domain/socket-lifecycle.js:53-57`) provably cancels a pending reconnect and the timer
callback is double-guarded — **so the survival proves `deleteSession()` was never called**. The bug is in the
*backend's* bookkeeping (`sessions.py:430-445`), not the gateway's lifecycle. Which of the three candidate
causes fired is **not yet determined**.

**F2 — ephemeral sessions are never reaped.** `whatsapp_private.gateway_sessions` = **34**,
`public.whatsapp_sessions` = **3**. 31 private rows have no public counterpart, all `is_active=false`,
overwhelmingly `session_name='Hat 1'` — abandoned pairing attempts.

**F3 — `event_outbox` cannot store ephemeral events.** FK `event_outbox_session_id_fkey → gateway_sessions`.
`registerSession` is gated `!ephemeral` (`session-manager.js:1121`) and so is the lease (`:2568`) — **the
outbox is not** (`events.js:224`). Result: **74 × SQLSTATE 23503 in 40 min**, all `session_qr_updated` /
`session_connecting`. Not data loss (the `catch` falls through to `deliverBestEffort`), but durability is lost
+ a failed statement each time. This is the **third consumer** of the same dependency missing the same guard.

**F4 — 1341 / 2289 outbox rows are permanently undeliverable.** 1611 DELIVERED, 678 DEAD_LETTER, 0 PENDING;
**1341 addressed to gateway sessions with no public row** (only 948 deliverable). Oldest 2026-09-14.

**F5 — transient event storm.** 22:16–22:21, `4ed446af-…` (`Hat 1`, inactive) produced **2065 dropped events
in one 60 s window**, each a WARNING from the rate-limited orphan logger. Now stopped (0 in last 60 s).
Mechanism inferred, not proven.

**F6 — the pairing map is in-memory** (`sessions.py:229-230` `_ephemeral_pairings` / `_logical_to_ephemeral`),
so **any backend restart orphans every live pairing** — the gateway session lives in a different process and
can never be deleted by token. This is the one finding the deploy *did* exercise (the backend was recreated).
Compounds with `get_session_qr` (`sessions.py:494-550`), which **creates a new ephemeral gateway session**
whenever a `RELINK_REQUIRED` logical session's ephemeral is missing, and **overwrites**
`_logical_to_ephemeral[row.id]` at line 544 — orphaning the previous entry. `whatsapp_sessions.id=5` **is**
`RELINK_REQUIRED` in production.

**No code changed.** Three fixes recommended in the report (gate the outbox on `!ephemeral`; reap orphan
`gateway_sessions`; make the pairing map durable / stop overwriting the mapping). The only way to kill the
looping socket today is `--force-recreate gateway` — a production write, **not performed, needs approval**
(it would also drop any other live session).

**New tooling insight** (added to the `asymmetric-resource-guard-detection` skill): the asymmetry is not only
acquire-vs-renew — grep **every** consumer of the dependency. The third-consumer fingerprint is *not* a kill at
`T+interval` but a **stable SQLSTATE count per event**, so count error codes rather than intervals. And a
resource that survives its own successful cancel proves the **release never reached it** — go read the caller's
bookkeeping, not the resource's lifecycle.




---

## "Bağlı bir WhatsApp hattı yok" — root-caused (READ-ONLY; report: `WHATSAPP_NO_CONNECTED_LINE_DIAGNOSIS.md`)

**The message has TWO producers, both backend, and the DOM one is NOT the 409.**

| Producer | Channel | String |
|---|---|---|
| `endpoints/whatsapp.py:51-61` `_no_session()` | HTTP **409** | diacritic `Bağlı bir WhatsApp hattı yok. Lütfen önce QR ile eşleştirin.` → toast |
| `orchestration/sync.py:1019-1022` | sync-job `error` → **HTTP 200** `GET /sync/job` + WS `whatsapp_sync_failed` | ASCII `Bagli bir WhatsApp hatti yok. Lutfen once QR ile eslestirin.` |

Rendered in the DOM: the **ASCII/sync-job** one, at `WhatsAppHubPage.tsx:1938` inside the rose
"Live Sync failed / Retry" banner (`:1922-1941`), first child of the Live Conversations left column
(`w-full md:w-80 lg:w-96`). DOM path fingerprint: `p.mt-1.text-[10px] < div.mx-3.mt-3 < div.w-full.md:w-80
< div.vuexy-card`. **Not a toast, not a dialog.**

Chain: `WhatsAppHubPage.tsx:1619` (mount) → `refreshSyncStatus()` `:450` → `:468`
`setSessionSync({phase:'error', error: job.error})`. Plus live path `:1464-1465`.
**`_sync_jobs` is an in-process dict (`sync.py:311-322`) and `get_sync_job` returns `snapshot()` regardless
of state ⇒ a FAILED job is retained and re-renders the banner on EVERY hub mount until replaced or the
backend restarts.**

**CASE B REFUTED (code + empirically).** `pairing/start` (`whatsapp.py:100-109`) catches only
`Exception→502`; it never calls `_no_session`. For a user owning **0** `whatsapp_sessions`:
`POST /pairing/start` → **201**, `GET /pairing/{token}/qr` → **200** with a real QR (208×208, natural 292).
**No "CONNECTED required" guard on the pairing path.** No-Create holds: 0 rows written; no pair-token table
exists (`_ephemeral_pairings` is in-memory, `sessions.py:229`).
**CASE A CONFIRMED** (message = truthful consequence of no CONNECTED line). **CASE C REFUTED** (the
0-session user saw it; the stale-row user did not).

### The real defect found: a phone paired, the promotion never landed
- Gateway `22:39:57` / `22:40:40`: `Baileys restartRequired (515)` for `37c4da78-…` and `89214b84-…`
  ⇒ **the real phone scan succeeded** (real JIDs `905413749073@s.whatsapp.net`).
- Gateway promoted both (`ephemeral=false` → `registerSession` wrote `gateway_sessions` rows at
  `22:40:00.368` / `22:40:43.657` → `session_connected` emitted) — but the **backend never created a
  durable `public` CONNECTED row**: grep `22:35–22:46` for
  `session_connected|Phase15.4|relink OK|RelinkCandidate|perform_atomic_relink` → **empty**.
- Backend instead: `Gateway olayi sahibi cozulemedi … Bilinmeyen gateway oturumu` (`events.py:1309`) —
  **2 904 events dropped in 60 s**.
- Both ephemeral pairings were cancelled within ~2–5 s (`POST /pairing/…/cancel` 200; `DELETE
  http://gateway:8787/sessions/89214b84-…` 200).
- Mechanism: `perform_atomic_relink` needs **both** phone **and** user_id (`events.py:1246`); with no phone
  it silently skips → P6-9 (`:1285-1307`) routes but **creates no row**; if the ephemeral entry is gone even
  P6-9 fails → `EventOwnerUnresolved`. The frontend's cancel removes that entry:
  `WhatsAppQrConnectModal.tsx:401-409` (effect cleanup), `:379-383` (close), `:192-195` (in-flight);
  effect deps `:410` = `[isOpen, initSession, clearTimers, existingSessionId]` ⇒ a re-run cancels a live
  pairing.

### State at 22:55:20Z
`public.whatsapp_sessions` 3 rows / **0 CONNECTED**; `gateway_sessions` 36 (1 active, **35 orphans**);
`socket_leases` **0**; `event_outbox` 3 291. `id=68` = `RELINK_REQUIRED`, `phone=+905076382749`,
`WHATSAPP_AUTH_RELINK_REQUIRED`.

**DISCLOSED SIDE EFFECT (not a manual edit):** my UI-driven Sync as `e512dd40` hit
`POST gateway:8787/sessions/a618f89c-…/conversations → 404` at `22:50:03`, so the app itself reclassified
`id=68` `CONNECTED → RELINK_REQUIRED`. The row was already a lie (`socket_leases` empty, `is_phone_online=f`).
No manual DB edit / session delete / status update / container recreate was performed. Only DB writes were
additive self-expiring rows in `public.auth_staging_sessions` (inserted, then expired immediately).

**Fixes proposed, NONE applied:** (1) stop cancelling a pairing whose socket reached
restartRequired/connecting; (2) make promotion not depend on the ephemeral registry — build the durable row
from the event's own phone/self_jid; (3) gate the sync-failed banner on a real CONNECTED session and show a
QR CTA instead; (4) clear retained FAILED jobs + reap the 35 orphans + the outbox `!ephemeral` guard;
(5) re-pair `id=68`.

**Harness note:** `/tmp/tz_repro_ns.mjs` (Chrome CDP + MutationObserver + `getResponseBody`) plus
`/tmp/tz_probe_job.sh` reproduced this end-to-end. Decisive trick: **search the DOM for the needle with a
MutationObserver, and compare the FOUND string's diacritics against every raise site** — the ASCII/diacritic
split immediately identified which of the five candidate producers fired.

---

## PHASE 6.8 — pairing promotion race: FIXED LOCALLY (not deployed)

Diagnosis above stands. The fix is now implemented, verified by falsification, and **not** committed,
pushed, or deployed (per the brief's §12). Report: `WHATSAPP_PHASE6_8_PROMOTION_REPORT.md`.

**Root cause — two independent defects on one chain, either alone loses the pairing:**
- **1a (backend):** `_map_session_event` could only **update** or **relink**; it had **no row-creating
  branch**. The only creator was `get_pairing_qr` → "Scenario A-new" ⇒ promotion silently depended on
  the browser continuing to poll — which the UI stops doing at exactly that moment.
- **1b (frontend):** the modal auto-closes `+1500 ms` after `session_connected`; the close re-ran the
  lifecycle effect whose cleanup called `cancelPairing(token)` **unconditionally** ⇒ `gw.delete_session`
  on a socket promoted 1.8 s earlier. Three unguarded sites: in-flight-create bail-out, close branch,
  effect cleanup (deps `[isOpen, initSession, clearTimers, existingSessionId]`).

**Falsification (the numbers that matter):**
| | HEAD | with fix |
|---|---|---|
| `test_whatsapp_phase6_8_promotion.py` (24 tests) | **15 failed, 9 passed** | **24 passed** |
| frontend race checks (modal reverted to HEAD) | `1 !== 0` FAIL | PASS |

The two "a genuinely waiting pairing IS still cancelled" guards pass on **both** revisions ⇒ the fix did
not merely disable cancellation.

**Fix shape (three parts, all needed):**
1. **New `orchestration/promotion.py`** — one idempotent `promote_ephemeral_pairing()` reachable **from
   the event alone**. **Owner resolved FIRST** (fail-closed, before any row query), then
   already-bound → update / `RELINK_REQUIRED` → atomic relink / row for phone → reuse / else **CREATE**.
2. **New `models/ephemeral_pairing.py` + `orchestration/pairing_registry.py`** — durable mirror of the
   in-memory `_ephemeral_pairings`, explicitly **best-effort, never a new hard dependency**. Built by the
   existing `create_all` in `main.py:103`.
3. **`WhatsAppQrConnectModal.tsx`** — explicit **monotonic** lifecycle
   (`CREATING → SCAN_QR → PAIRING_IN_PROGRESS → PROMOTION_PENDING → CONNECTED`), held in a **ref**;
   `CANCELLABLE_PAIRING_STATES = ['CREATING','SCAN_QR']`; one idempotent
   `cancelPairingIfStillWaiting()` replacing all three sites. `cancel_pairing_session` is now
   **promotion-aware**: if the gateway says `CONNECTED` it finalises and does **not** delete the socket.

**Three bugs in my own fix, caught by the full suite / real browser — worth remembering:**
- Promotion ran **before** owner resolution and trusted whatever `db.scalar` returned ⇒ broke
  `test_map_session_event_fails_closed…` and `test_session_connected_rebinds_recovered_session`.
  Fix: owner first + `_to_dict` must reject any object that is not a real `EphemeralPairing`.
- `consume_pairing` failure → `rollback()` → ORM row **expired** → bare `row.id` read raised
  `greenlet_spawn has not been called` → an **already-committed** promotion was dropped by
  `ingest_gateway_event`. Fix: capture `row_id` first, wrap in try/except, refresh inside `await`
  with a PK re-read fallback.
- `handleCancel` marked `CANCELLED` **before** the guarded cancel ⇒ the guard blocked its own
  cancellation. **jsdom did not see it; only real Chrome did.** Fix: cancel first, then mark terminal.

**Gates (all measured):** backend **1113 passed / 4 skipped / 0 failed** (baseline 1089 + 24 new);
gateway **22/22**; `tsc --noEmit` 0 errors; logic 43, dom 20, browser 7/7, **pairing 12** (was 8),
**pairing-browser 13/13** (was 12), chat-order 10/10, merge + merge-equivalence PASS.
Also repaired pre-existing harness rot in `test-whatsapp-chat-order.mjs` (verified broken at HEAD).

**NOT RUN / NOT DONE:** `LIVE DEVICE E2E = NOT RUN` — no live phone, and §12 forbids the production
actions it needs. §7 real event chain w/ timestamps, §11 real Chrome vs **real** gateway, §11 real phone
scan → CONNECTED → lease, §9 post-connect chat smoke, live outbound re-measure. **The phase is therefore
NOT marked complete.** §10 cleanup items left as findings only (35 orphan `gateway_sessions`, outbox
FK 23503 for ephemeral, 1 341 undeliverable outbox rows, in-memory registry, retained FAILED sync jobs).

**New skill saved:** `client-lifecycle-cancels-server-promotion` (the general pattern: a durable row whose
only creator is client polling + a client lifecycle event that cancels the already-succeeded resource).

### Phase 6.8 — REVIEW PASS (the §12 "final diff has been reviewed" precondition)

Full diff read (10 tracked files, +622/−61, plus 4 new untracked source files). Four things came out of it:

1. **Latent trap in the guard predicate — HARDENED, not a bug fix.** The guard was a *negation over a
   non-complementary set*: `if (!CANCELLABLE_PAIRING_STATES.includes(state)) return;` with
   `CANCELLABLE = ['CREATING','SCAN_QR']`. `IDLE`/`CANCELLED`/`FAILED` are in neither set, so the negation
   silently classified them as **promoted** — at *both* guard sites. Replaced with the positive
   `PROMOTED_OR_PROMOTING_STATES = ['PAIRING_IN_PROGRESS','PROMOTION_PENDING','CONNECTED']`.
   **Reachability was NOT proven** — those three states cannot currently be held while a pair token exists
   (see 2), so nothing was actually broken. It was one small change away from going live, hence corrected.
   Reported as hardening; must not be described as a fix.

2. **NEW FINDING (→ §10 cleanup track, deliberately NOT fixed): an ephemeral pairing never learns the
   gateway went terminal.** *Every* `applyTerminalGatewayStatus` call site is on the **existing-session**
   path — the poll's `pToken` branch only has `else if (res.error_message)`, and `handleRefreshQr`'s
   `currentPairToken` branch has no terminal call at all. So `BANNED`/`UNAVAILABLE`/`RELINK_REQUIRED` during
   an ephemeral pairing leaves the modal showing a QR that can never work until the 25 s countdown or a
   manual refresh. Pre-existing, separate change, not bundled.

3. **I wrote a vacuous test and caught it.** Added a `TERMINAL:` check for (1); falsified it by deliberately
   restoring the buggy predicate → suite still reported `PASS (14 checks)`. It drove `FAILED`, which is
   unreachable (2), so it asserted nothing. **Removed**, with an inline note so it isn't re-added before (2)
   is fixed. `verify:pairing` is back to **12 checks**. Lesson written into the skill: *restore the bug and
   re-run — if it still passes, delete the test.*

4. **Cosmetic + documentation:** two statements merged on one line in `verify-whatsapp-pairing.mjs`; four
   stray blank lines and a duplicate `D:` check label in `verify-whatsapp-pairing-browser.mjs` (→ `G:`);
   `pairing_registry.resolve_pairing_by_token` documented a production QR-poll caller that was **reverted**
   — it is test-harness-only and must never be used in an owner-resolution path (it includes consumed rows).

**Verified during review (things that would have been silent failures):** `_to_dict` maps
`gateway_session_id` → `"gateway_id"`, which is exactly what `cancel_pairing_session` reads — a name
mismatch would have turned every durable-path cancel into a silent no-op. `reconcile_self_identity(db,
user_id, session, self_lid=)` exists with that signature. `isCancelledRef` **is** reset on reopen, so the
new `handleCancel` early-return cannot latch and block a later pairing. No debug leftovers, no secrets, no
existing test weakened.

**Gates re-run after the review changes:** `tsc --noEmit` 0 · `verify:pairing` **PASS (12)** ·
`verify:pairing-browser` **13 passed / 0 failed**. Backend untouched (docstring only in
`pairing_registry.py`) ⇒ **1113 passed / 4 skipped** still stands.

**Still `LIVE DEVICE E2E = NOT RUN`.** Nothing here changes that. No commit/push/deploy.

### Phase 6.8 — FINAL-REVISION GATE SWEEP (every suite re-run on the shipped code)

| Gate | Result |
|---|---|
| Backend full suite | **1113 passed / 4 skipped / 0 failed** |
| `tsc --noEmit` | 0 errors |
| `verify:logic` / `verify:dom` / `verify:browser` | 43 / 20 / 7 passed, 0 failed |
| `verify:pairing` / `verify:pairing-browser` | 12 checks / 13 passed, 0 failed |
| Gateway | 22/22 (gateway code untouched by this phase) |

**Environmental trap re-confirmed — a raw run is misleading.** `pytest backend/tests/` straight against the
dev DB gives **28 failed, 1085 passed**. Cause: `tezlify.db` holds a leftover `CONNECTED` row (`id=2`,
owner `67890123678967896789678901234567`) ⇒ together with each test's seeded owner that is
`bagli tenant sayisi=2` ⇒ `EventOwnerUnresolved` ⇒ the product fails closed, correctly. Deleting that one
row **in a copy** → 0 failed at an **unchanged total** (28+1085+4 == 1113+4), which is the proof it is
environmental and not a regression. The copy also held **11** stale `ephemeral_pairings` rows from earlier
in-place runs — one more reason never to point pytest at the dev DB. Exact command now in `MEMORY.md`.

**State: no commit, no push, no deploy. `LIVE DEVICE E2E = NOT RUN`.**

**Sweep completed — the remaining three frontend suites on the final revision:** `verify:merge` PASS
(identity/status/reconnect retention; 1200+pending merge in 1.22 ms) · `verify:merge-equivalence` PASS
(10 rows, mode=canonical) · `test-whatsapp-chat-order` **10/10 PASS**. With logic/dom/browser/pairing/
pairing-browser already re-run, the frontend set is complete, so the report's "every suite re-run on the
final revision" claim is now literally true.

**Gotcha found while enumerating the gates:** `frontend/package.json` has only **7** verification scripts
(`verify:logic`, `verify:dom`, `verify:merge`, `verify:merge-equivalence`, `verify:browser`, `verify:pairing`,
`verify:pairing-browser`). `test-whatsapp-chat-order` is **not** among them — it only runs via
`node scripts/test-whatsapp-chat-order.mjs`. Anything that loops over the npm scripts silently skips it.

### Phase 6.8 — FINDING 7 FIXED (separate scoped follow-up, taken after 6.8 was frozen)

Cleared MEMORY's "Terminal-status follow-up in progress" line. **Three frontend files only**;
no backend/gateway/production change. Full record in report §12.

**The handed-over tree was mid-work and live-broken.** The finding-7 call sites WERE present
(ephemeral poll + ephemeral refresh), but `PROMOTED_OR_PROMOTING_STATES` still held the
six-state complement (`IDLE, PAIRING_IN_PROGRESS, PROMOTION_PENDING, CONNECTED, CANCELLED,
FAILED`) — §11.1's documented 3-state replacement was not actually in the tree. With `FAILED`
newly reachable (that is what the call sites do), the six-state form stopped being latent:
Cancel/close on a terminal pairing would **claim CONNECTED** (a §1.1 violation) and leak the
socket. The restored TERMINAL checks failed in vivo on the first run (`cancel=0`) — that
failure is itself the falsification of both checks and the proof the defect was live.

**Fix:** predicate → documented 3-state set (`PAIRING_IN_PROGRESS`, `PROMOTION_PENDING`,
`CONNECTED`); kept the two call sites; restored the two TERMINAL checks; added a `!qrImg()`
assertion that pins the mechanism itself — **without it the checks pass even when the call
site is missing** (the cancel guard stays satisfiable because a never-terminal pairing is
cancellable anyway). That gap was found while designing the browser check, not by the suite.

**Falsifications, all in vivo (restore-the-bug discipline):**
1. six-state predicate → both TERMINAL checks FAIL (`cancel=0`).
2. `false && applyTerminalGatewayStatus(...)` probe = pre-finding-7 behaviour (harness sends
   `error_message: null`) → jsdom FAILs at "the dead QR must be removed once the gateway is
   terminal"; real-Chrome `H` FAILs with the same message and the dead QR's data URI in `qrSrc`.
3. Chrome then caught a bug in my OWN new check: `cancel === 0` asserted right after `reset()`
   collides with the previous check's *correct* unmount-cancel → assert against an explicit
   `cancelsBefore` baseline. Test fixed; product behaviour was right. (Chrome has now caught
   both a product bug jsdom missed — §11 handleCancel ordering — and this test bug.)

**Gates on the combined final revision:** backend **1113 passed / 4 skipped** (re-run on the
session-free copy), `tsc` 0, logic 43, dom 20, browser 7/7, **pairing 14**, **pairing-Chrome
14/14** (was 12/13), merge PASS, merge-equivalence PASS, chat-order 10/10, gateway 22/22
untouched. `LIVE DEVICE E2E = NOT RUN` unchanged. No commit, no push, no deploy.

**Lesson worth keeping:** before trusting a "restored" regression check, ask *what does it
assert that the broken code cannot satisfy?* If the answer is only "a cancel didn't happen",
check whether the broken mechanism even affects cancel-counts — here the missing call site
did not, until the QR-removal assertion was added. And a check that reads counters after a
shared-harness reset must capture a baseline, never assume zero.
