# 2026-09-26

## Production frontend deploy — chat-thread singleton fix (`5916ff1`)

Shipped the fix for the stacking ChatThread bug (the one visible in the user's screenshot: several
chat panes stacked inside one hub Card, each contributing its own scroll area).

**Why it stacked (diagnosed 2026-09-25, from live console probes):**
The pane held N `ChatThread` fibers whose keys were the conversation ids the user had clicked
(`13795/13794/13796/13781`), under a single Card and a single 112-row sidebar, with **no error
thrown**. The deployed bundle had exactly one `ChatThread` call site (no `map`) and the source had no
imperative DOM movement, React production 18.3.1, `<font>` count 0 (so no translator/extension
mutation). Culprit: `key={selectedConv.id}` destroyed and rebuilt the whole thread subtree on every
conversation switch; when the teardown did not complete, the old root stayed in the pane and each
click appended another. One instance reported `msgCount: 0` while still rendering messages — direct
proof the fiber tree and the live DOM had desynchronised.

**Fix:** `ChatThread` gained a `conversationKey` prop. Identity change no longer destroys the
instance; it resets per-conversation state in the render phase (scroll/pin/guide state, near-bottom,
pill, prepend guards, K19 height cache/window/anchor/pin refs) and re-takes the opening position via
`convEpoch`. `hasContent` added to the initial-positioning effect deps — the gap where a message
arriving into an empty chat with no loading transition never took its opening position was
previously masked by the remount. `WhatsAppHubPage.tsx` uses `conversationKey={selectedConv.id}`
(the composer stays keyed for draft isolation); `LeadDetailDrawer.tsx` passes `conversationKey={lead.id}`.
A static contract in three verify files fails if `key=` returns.

**Deploy performed (approved by user):**
- Pre-flight: clean tracked tree, `HEAD == origin/main == 5916ff1`.
- Built off-box: `rm -rf dist && npm run build` → `index-3UEtgr4K.js` (879.16 kB), `index-5YKannVK.css`.
  No `frontend/package-lock.json` exists, so `npm ci` is structurally impossible — existing
  `node_modules` was reused.
- Uploaded to new release `/opt/tezlify/releases/v20260926_phase6_8_chatthread_singleton` via
  `rsync -az --delete`. sha256 matched local↔remote end-to-end.
- Atomic swap: `ln -sfn … frontend_candidate.new` + `mv -T`, then
  `docker compose -f docker-compose.prod.yml up -d --force-recreate --no-deps caddy`.
- Result: container sees `index-3UEtgr4K.js`; `curl https://localhost/ -H "Host: …"` → HTTP 200 and
  the served asset hashes to the same `670fe140…`. Caddy healthy. Backend/gateway `StartedAt`
  unchanged (2026-09-21T19:57) → `--no-deps` genuinely scoped, WhatsApp socket preserved.

**Verification:** build ✓ · verify:logic 43/43 ✓ · verify:dom 21/21 ✓ · verify:browser real Chrome
8/8 ✓ — including `H`: opening 6 conversations in a row leaves exactly one thread root in the pane.

**Rollback:** `ln -sfn /opt/tezlify/releases/v20260919_phase6_5_lid_scope_and_lease frontend_candidate.new && mv -T frontend_candidate.new frontend_candidate` then force-recreate caddy. Old release retained.

**Notes:** release dirs are `/opt/tezlify/releases/` (not `/opt/tezlify/frontend/releases/`, an older
unused path). Only one frontend site block, so it also answers an unmatched Host. Cache headers were
already right (entry doc `no-store`, hashed assets `immutable`) — a plain reload suffices.

## Post-deploy production probe (user reported it "still broken")

User reported the pane still splitting on every click. Ran a real-Chrome probe against
`https://app.tezlify.com` with an ephemeral token (`scripts/auth_helper.get_ephemeral_auth_token`,
2-hour `auth_staging_sessions` row). Verdict: **the stacking is gone in production.**

- Loaded bundle confirmed `assets/index-3UEtgr4K.js` (the new one).
- Mounted `ChatThread` instances after each of 6 conversation switches: **1, 1, 1, 1, 1, 1, 1**.
  Exactly 1 scroll container, `atBottom: true` every time, zero page errors.
- Screenshots confirm a single pane: "Hakan Aksu" renders 54 real bubbles with media and ticks.
- Instances were counted **name-independently** (walk the fiber tree from the `__reactContainer$`
  root and collect fibers whose `memoizedProps` have a `messages` array + `leadName`) because
  minified component names change between builds — do not hardcode `t0`.
- Conclusion: the user's tab was still running the pre-deploy JS. A SPA keeps the old bundle in
  memory until reload; confirm with `document.querySelector('script[src*="/assets/index-"]').src`.

**Separate pre-existing defect found (NOT caused by this deploy — static files only):**
`GET /api/v1/whatsapp/conversations/{id}/messages` returns **502** for conversations whose local row
count is below the page size. Chain: local rows < `page_size` -> on-demand provider hydration ->
gateway `?fetch_provider=true` returns **HTTP 200 but takes ~15,013-15,016 ms** -> backend's 15 s
ceiling trips -> `WhatsAppHistoryTimeout: Provider history chunk timed out for <jid>` -> endpoint maps
it to 502 (deliberately, "retryable state, not an empty conversation"). Observed on conversation
13907 (8 stored rows) and jids `6282245414751`, `905354931592`, `905413749073`; conversations with
>= 50 stored rows (13908/13909/13910) return 200 and never hit the gateway. So the timeout budget is
the mismatch, not the UI. UI behaviour is correct: skeletons while `loading`, then an
`EmptyState` + retry once `messageLoadState` flips to `error`.

**Trap re-hit:** used `grep "a\|b"` and got an empty result, briefly reading it as "no ChatThread in
the hub page". This is the documented BSD-grep BRE alternation trap — use `grep -E` or the Grep tool.

## PRODUCTION INCIDENT — WhatsApp line deleted, all conversations/messages lost (2026-09-25 23:17:04 UTC)

While investigating the 502, discovered that `conversations` (5067 ins / 5039 del) and `messages`
(45362 ins / 54576 del) were **empty**, with `max(id)` NULL. Cause, from the backend access log:

```
23:17:04  DELETE http://gateway:8787/sessions/eeacbbcf-88b1-4227-8687-ab760e445d71  -> 200
23:17:04  DELETE /api/v1/whatsapp/sessions/81  (from 104.28.244.150 = Cloudflare edge) -> 200
```

Deleting a line cascades to that line's conversations and messages (`Conversation.messages` is
`cascade="all, delete-orphan"`), and **all 111 conversations belonged to session 81**
(+905413749073) — so the whole WhatsApp dataset went with it. The gateway's session dir
`eeacbbcf-…` was also removed from the `tezlify_whatsapp_sessions` volume, so the line's Baileys
credentials are gone too.

**Who:** a browser client, not the probe. Verified my probe scripts contain no `delete`/`sessions`
calls (they only open the hub tab and click `button[data-conv-id]` rows). The log line immediately
preceding the DELETE is `GET /api/v1/whatsapp/sessions` — a screen the probe never opens. The delete
UI is a `Trash2` icon on `SessionCard.tsx` gated by `whatsapp.deleteSessionConfirm` ("…All synced
conversations and messages will be deleted as well. This action cannot be undone.").

**Damage / what survived:**
- Lost: all conversations + messages (no backup covers them), session 81 row, gateway auth state.
- Survived: `contacts` 1401, `lid_mappings` 10433, `event_outbox` 6605, `leads` 3,
  `whatsapp_sessions` 2 (old, SCAN_QR/RELINK_REQUIRED), media volume 73 MB / 29 files.

**Recovery is NOT possible from backups — proved, do not assume otherwise:**
- Sep 19 `predeploy_20260919_215146/db.dump` (4.6 MB): **schema-only** for `conversations`/`messages`
  (TABLE entries, no TABLE DATA entries). A non-trivial size does NOT imply usable content.
- Sep 16 `backups/postgres/*.dump` (31 MB): restored into a scratch DB — valid `PGDMP`, restores
  cleanly, but `conversations` = 0 and `messages` = 0 (those tables were empty on Sep 16).
- `archive_mode = off`, no replication slots → no PITR.
- Restoring a scratch DB requires `pg_restore -U tezlify`; without `-U` it tries OS user `root` and
  fails with `role "root" does not exist` (same trap as psql). Scratch DB was dropped after use.

**Consequence:** the WhatsApp line must be re-linked by scanning a QR code from the phone; the
message history cannot be restored. The 502 investigation is moot until the line is re-linked and
conversations re-sync.

## 502 fix — implemented, verified, awaiting deploy approval

User reconnected the line ("hattı bağladım"), so the 502 became actionable again. Implemented the fix
and closed out the suite.

**The fix** (3 files, uncommitted): `_hydrate_or_tolerate_provider_timeout` in `whatsapp_service.py`
wraps the single provider round-trip; `is_provider_recently_unresponsive` +
`PROVIDER_UNAVAILABLE_COOLDOWN_S = 300` in `history_evidence.py`, plus `last_attempt_at` threaded
through `get_history_evidence`. Three guarantees, H-3 untouched:
(a) timeout with `have_rows=True` → serve local rows instead of discarding them;
(b) recent TIMEOUT/PROVIDER_ERROR + local rows → skip the futile round-trip;
(c) zero rows → still raises → still 502, never a false "empty".

**Falsified, not just asserted:** stashed the fix and re-ran — test_21 failed with the exact
production exception (`WhatsAppHistoryTimeout`), 23/24 failed on the new API, 22 passed both (it
guards the unchanged contract). With the fix all 4 pass.

**Wiring checked, because a green test can hide a dead path:**
- `sync.py` commits the TIMEOUT evidence *before* raising, so the cooldown really arms.
- `AsyncSessionLocal` sets `expire_on_commit=False` (database.py:51), so the commit inside the
  hydrate path does not expire the already-loaded `rows` — otherwise the tolerated path would have
  turned a 502 into a `MissingGreenlet` 500. The test harness uses the same flag.
- `last_attempt_at` exists in the live schema *and* has `ADD COLUMN IF NOT EXISTS` (migrations.py:1651),
  so the predicate is not silently fail-open dead code.

**The one red test was a test-isolation artifact, not the fix.** `test_ingest_message_new_degenerate_no_conv`
asserts a **global** count of WHATSAPP conversations is 0, while its own `_cleanup` wipes only two
synthetic users — so any foreign WhatsApp data fails it. Proof, in order:
- Same failure `assert (2505 == 0)` on **baseline** code (fix stashed) → not caused by the change.
- On a **schema-only** empty DB, that module is 20/20 and the **full suite is 1162 pass / 4 skip / 0 fail**.
Build that DB with `sqlite3 tezlify.db .schema | sqlite3 /tmp/empty.db` — a bare `create_all` gives only
16 tables and omits the raw-SQL ones (`history_sync_states`, `lid_mappings`, `auth_staging_*`).

**Production state after the relink:** 112 conversations, **0 with >= 50 rows** (93 in 1-49, 19 at 0).
So the vulnerable bucket is now 100% of conversations: (a) covers the 93. The 19 zero-row ones still
502 by design — 17 have no evidence row, 4 NOT_CHECKED, only 2 NO_MESSAGES, and
`is_history_exhausted_or_stalled` does not count `NO_MESSAGES` as exhausted. TIMEOUT rows were ~24 min
old, i.e. past the 300 s cooldown, so a first open still pays ~15 s before rows render.

**Not deployed.** Backend deploys need explicit user approval. Nothing else was touched.

## 502 fix deployed to production (2026-09-25 23:46 UTC, commit `9981298`)

User approved the deploy. Pushed `5916ff1..2ae4549` (fix + memory docs), then deployed.

**Why NOT a plain `git pull`:** prod `/opt/tezlify` is a clean checkout on `main` sitting at `359fe6d`,
four commits behind. `359fe6d..HEAD` also carries `backend/app/services/whatsapp/orchestration/sessions.py`
(+160), `orchestration/sync.py` (+140) and `whatsapp_gateway.py` (+14) from `ddbfbf4` — session-lease
and gateway-client changes, exactly the risky kind for a live WhatsApp system, and *not* what was
approved. Checked first that prod's `whatsapp_service.py` and `history_evidence.py` are byte-identical
to HEAD's, which makes the fix self-contained and applicable to the older tree. So deployed a **file
subset**:

```
git fetch origin
git checkout origin/main -- backend/app/services/whatsapp_service.py \
    backend/app/services/whatsapp/orchestration/history_evidence.py \
    backend/tests/test_whatsapp_history_orchestration.py
docker compose -f docker-compose.prod.yml build backend
docker compose -f docker-compose.prod.yml up -d --no-deps backend
```

- sha256 of all 3 files matched local exactly (27993271…, 2ac994e0…, 4c123dcb…); `git status` showed
  exactly those 3 staged and nothing else. HEAD deliberately left at `359fe6d` — the dirty tree *is*
  the deploy marker.
- `--no-deps` is mandatory here: the gateway runs `WHATSAPP_AUTO_RESTORE: "false"`, so restarting it
  would strand the linked line. Verified scope via `StartedAt`: backend `2026-09-25T23:46:28` (new),
  gateway `2026-09-21T19:57:39`, db `2026-09-15T20:55:36`, caddy `2026-09-25T23:03:36` — only backend.
- Build context is 139 MB and there is **no `.dockerignore`**; harmless because the build runs on the
  server, and the `pip install` layer stays cached (only `COPY backend/` invalidates).

**Post-deploy verification (session 82, `CONNECTED`, +905413749073):**
- Backend healthy; log shows `[WS-GATEWAY] Baileys gateway bağlandı.` → the gateway re-handshook by
  itself, and `whatsapp_private lid_mappings & history_sync_states verified` on migration.
- conv 14010 (16 rows) → **200 in 2.6 s**, hydrated to 50, evidence `HAS_MORE`.
- conv 14045 (15 rows) → **200 in 15.04 s serving its own 15 rows**, evidence `TIMEOUT` — previously
  this exact request was a 502. Log: `Provider history timed out (conv=14045); serving local rows
  instead of failing`.
- Immediate re-request of 14045 → **200 in 0.024 s** (600× faster). Log: `On-demand provider request
  skipped (conv=14045): provider recently unresponsive`. Cooldown works.
- Sweep of 6 conversations (all in the 1-49 row bucket, i.e. all previously 502) → **all 200**, no 5xx.
- `provider_exhausted` stayed `f` on the timed-out jid → **H-3 held in vivo**, a timeout did not become
  exhaustion.

**Still broken by design:** the 19 zero-row conversations keep returning 502 — 17 have no evidence row,
4 `NOT_CHECKED`, only 2 `NO_MESSAGES`, and `is_history_exhausted_or_stalled` does not count
`NO_MESSAGES`. Offering an "empty but unverified" response is the follow-up if the user wants 0/112
instead of 19/112. A *first* open of a timed-out conversation still pays ~15 s.

## End-to-end UI verification in production (real Chrome, 2026-09-26 02:53–02:58)

An API-level 200 is not enough for a complaint that was UI-level ("the pane doesn't work"), so the
chat pane was driven in real Chrome against `app.tezlify.com` with an ephemeral token.

**Verdict: the pane works. Every conversation tested rendered its messages.**
- Exactly **1** ChatThread fiber on every click (`PASS_single_thread`), so the singleton fix holds.
- **No 5xx and no error state** on any click; `consoleErrors` empty.
- conv 14007 (48 rows) → 48 bubbles in ~300 ms, stable.
- conv 14020 (15 rows) → 15 bubbles after ~15.2 s (provider timeout, then local rows — previously a 502).
- conv 14023 (15 rows) → **first** visit pays ~15 s; the **repeat** visit renders 15 bubbles in
  **308 ms** and holds for 22 s. The cooldown is visibly working in the UI, not just in the API.
- conv 14010 / 14022 (50 rows) → 50 bubbles in ~200 ms (>= page size, no provider call).

**Probe pitfall that cost three rewrites — record it:** classifying the pane by
`document.body.innerText.includes('No WhatsApp Messages')` is **unreliable**. That string stays in the
DOM *while messages are rendered*, so it produced false "EMPTY_STATE" verdicts on conversations whose
own screenshots showed 15–48 bubbles. It even flips True/False between consecutive samples on the same
conversation. Two more traps: polling right after `click()` catches the pre-load frame (skeletons +
`messages: []`) and reports a bogus 1 ms "empty"; and a single sample can disagree with a screenshot
taken 200 ms later.

What actually works — sample a **timeline** every 250 ms and trust counters that cannot be faked:
- fiber census `msgCount` (the `messages` array the component really received) — authoritative;
- DOM bubble count via `div[class*="max-w-[85%]"][class*="px-4"][class*="py-2.5"]` (ChatBubble's root);
- `div.overflow-y-auto.space-y-1` presence + its `innerText.length` (the scroller only exists when rows
  are rendered, so `-1` distinguishes "no scroller" from "scroller with no text").
Require the state to hold across consecutive samples before classifying, and remember that a 15-row
conversation legitimately sits in the loading state for ~15 s on a first visit — do not call that a
failure.

**Residual confirmed as low-impact:** the 19 zero-row conversations are all *stale* — newest
`last_message_at` 2026-08-27, oldest 2026-03-24, 4 with no timestamp at all — and none appear in the
top 8 by recency, which all hold 15–51 rows. So they are not in the user's active path.

## Remaining UX issue: the ~15 s first-open wait (diagnosed, NOT changed)

The one user-visible symptom left is latency, and it is the literal reading of the original complaint
("every time I click it tries to load new messages"). Opening a conversation with fewer than
`page_size` local rows blocks the **first paint** on a "fetch older messages" round-trip.

Exact chain, traced in source:
- `backend/app/services/whatsapp_service.py:761` — `len(rows) < page_size` → the `else` branch runs even
  when `before is None` (i.e. a plain open, not a pagination), so the open path calls the provider.
- `sync.py:1825` — `gateway_client.get_messages(..., fetch_provider=True)`.
- `whatsapp_gateway.py:222` — `get_messages()` sends only `limit/before/fetch_provider/oldest_msg_*`;
  it has **no** `timeout_ms` parameter.
- `whatsapp-gateway/src/index.js:389` — the `GET /messages` route reads exactly those query keys and
  **does not read `timeout_ms`**, so `session-manager.js:660`'s hardcoded `timeoutMs = 15000` always
  applies. (`POST /conversations/:jid/history` at `index.js:411` *does* honour `timeout_ms`, and the
  backend client's `request_older_history()` already exposes it — the GET path is the gap.)
- Result: gateway waits 15 s for the phone, returns 200 with `provider_status=TIMEOUT`, the backend's
  `_hydrate_or_tolerate_provider_timeout` serves local rows. So it is a **wait**, not an error.

Measured: provider answered 5× (fast, ~2.6 s, `HAS_MORE`, `timeout_count 0`) and timed out 5× (15 s) in
one hour — roughly a coin flip per conversation, decided by whether the phone answers the history PDO.
`WHATSAPP_GATEWAY_TIMEOUT` (30 s) is the httpx timeout to the gateway and is *not* the binding limit.

Why it was left alone: every fix needs a **gateway** change (the GET route must forward `timeout_ms`),
and the gateway runs `WHATSAPP_AUTO_RESTORE: "false"`, so restarting it does not re-attach the live
line. `POST /sessions/restore` (`index.js:240`) can re-attach from persisted creds without a QR, so a
restart is *recoverable* — but after the 2026-09-25 data-loss incident and a fresh re-link, that risk
was not taken unilaterally. Options: (a) gateway route forwards `timeout_ms` + backend sends a short
value (~4 s) for the open path, keeping 15 s for explicit pagination; (b) stop blocking first paint at
all and hydrate only on real pagination (`before is not None`) — backend-only, but it changes tested
hydration semantics; (c) leave as is — keep the phone online and hydration is usually fast.

## RESOLVED: the ~15 s first-open wait is now bounded to ~4 s (verified in production)

Option (a) was chosen by the user and shipped as `6c7214a`. The GET route in
`whatsapp-gateway/src/index.js:389` now reads `timeout_ms` and forwards
`timeoutMs: parseInt(timeout_ms, 10)`; `whatsapp_gateway.get_messages()` gained
`timeout_ms`; `sync._hydrate_messages_on_demand()` gained `provider_timeout_ms` and forwards it;
`whatsapp_service.get_messages()` passes `OPEN_PATH_PROVIDER_TIMEOUT_MS = 4000` **only when the
request's own `before` cursor is absent**, i.e. a plain open. An explicit "load older" keeps `None`
→ the gateway's 15000 default.

**Where the budget must be decided.** Not inside `_hydrate_or_tolerate_provider_timeout` /
`_hydrate_messages_on_demand`: in that branch `before_ts_ms` is *always* set (it is the oldest local
row's timestamp), on both the plain-open and the pagination path, so it cannot discriminate. Only the
call site knows whether the *request* carried a cursor. (This cost one failed test: `test_25` asserted
`None == 4000` until the decision moved out.)

**Deploy:** partial-file checkout of 4 files (`whatsapp_service.py`, `whatsapp_gateway.py`, `sync.py`,
`index.js`) + `docker compose up -d --no-deps backend` and `... --no-deps gateway`. Backend
`00:11:14`, gateway `00:12:16`; db and caddy untouched. Gateway restart does **not** re-attach the
line (`WHATSAPP_AUTO_RESTORE=false`), so `POST /sessions/restore` was required — it re-attached
without a QR: `{"status":"ok","discovered":1,"restored":1}`, session
`4b69b1c0-50d8-4fb7-a96d-4e032811f1a8` CONNECTED `+905413749073`.

**Measured, from the production host (the sandbox proxy 502s on `app.tezlify.com`; Caddy is local
there, so the probe runs over ssh):**

| path | conversation | provider outcome | latency |
|---|---|---|---|
| plain open, 15 rows | 14021 / 14032 / 14059 / 14026 | TIMEOUT | **4189.7 / 4047.0 / 4051.7 / 4045.2 ms** |
| plain open, repeat ≤300 s | same | cooldown skip | 21–57 ms |
| explicit pagination (`before=89176`) | 14082 | TIMEOUT | **15046.5 ms → 502** |
| plain open, provider answered | 14105 | OK | 2424.8 ms |

Gateway log confirms it: "Older history request timed out waiting for provider chunk" fires for
905064709610/905078267662/905387844422/905525372434 (the four opens, ~4.5 s apart) **and** for
905359361452 (14082's pagination) — while 14105's open produced no timeout line, i.e. the phone
answered. `provider_exhausted = False` throughout → H-3 intact.

**UI confirmation** (real Chrome, fiber census + DOM count, 250 ms timeline): conv 14020 0→15
bubbles at **4387 ms**, 14024 at **4382 ms**, 14025 at **4376 ms**; `threadCount = 1` for the whole
timeline in every run (the singleton fix holds); skeletons visible during the wait, so the user sees a
loading affordance rather than a false "empty".

**Pagination still 502s on a provider timeout — deliberately.** With a cursor the base query returns
0 rows, so `have_rows=False` and the timeout is re-raised. Returning an empty page would tell the UI
"no older messages" and stop pagination forever (§1.1 no false positives); 502 is retryable. This is
pre-existing and unchanged by the budget work (it also used 15000 before).

**Do not trust `document.body.innerText.includes('No WhatsApp Messages')`.** In the loading state the
string is already present, so it reads `true` while the pane is *about to* render 15 messages. The
fiber `msgCount` and the structural DOM count are the ground truth; the screenshots agree with them,
the innerText check does not.

**Sandbox notes:** the local sandbox has `HTTP_PROXY/HTTPS_PROXY=http://127.0.0.1:59508`, which
tunnels `app.tezlify.com` into a 502 — use the production host for API probes. Playwright lives only
in the repo `venv` (`venv/bin/python`), not in the managed Python. Production is **PostgreSQL**, not
SQLite: the `tezlify.db` at the repo/host root is stale, `sqlite3` is not installed on the host, and
`history_sync_states` is keyed `(session_id, jid)` — so joining on `jid` alone duplicates rows across
sessions and inflates `count(m.id)`. Always aggregate messages in a separate subquery.

## Sohbet isimleri — kök neden bulundu (grup gönderen etiketi)

Kullanıcının "sohbetlerde isim sorunu" dediği şey iki ayrı durumdan oluşuyor; yalnızca **birincisi
gerçek bir hata**.

**1) GERÇEK HATA — gateway kişi deposu kalıcı değil.** Grup mesajının gönderen etiketi
`whatsapp-gateway/src/session-manager.js:1584 _resolveDisplayName()` ile üç adımda çözülür:
`store.contacts` adı → `msg.pushName` → telefon. Depo `messages/message-store.js:12` içinde
`contacts: new Map()` olarak **yalnızca bellekte** oluşturulur; diske yazılmaz ve açılışta yeniden
yüklenmez. `WHATSAPP_AUTO_RESTORE=false` olduğu için gateway yeniden başlatmaları olağan — her
seferinde depo boşalır ve `pushName` taşımayan her mesajın etiketi ham telefona düşer.

Kanıt (canlı, 2026-09-26 00:12 restart sonrası):
- `GET /sessions/{sid}/contacts` → **0 kişi** (adı DB'de duran kişiler dahil).
- Grup 14010: restart öncesi mesajlarda `sender_name` = "Cevat Aydın" / "Tolga Cebeci"; restart
  sonrası (00:25, 00:27) = `+905076382749`. 00:23'teki tek mesaj "Lazury" — o mesaj `pushName`
  taşıdığı için kurtuldu. Aynı kişi, aynı sohbette, iki farklı etiketle görünüyor.
- Etiket `ChatBubble.tsx:65-78` içinde `message.sender_name` + `message.sender_phone` üzerinden
  çözülür — yani **kişi kaydından değil, mesaj satırından**. DB'deki kişi adı bu etiketi düzeltmez;
  bozulmuş satırlar yeniden senkronlanmadan düzelmez.

**2) HATA DEĞİL — 8 sohbet başlıkta telefon gösteriyor** (14039, 14041, 14042, 14055, 14056, 14058,
14064, 14067). `display_name` = telefon, `name_source = "history"`. Hiçbir yerde ad yok: mesaj
`sender_name`'leri ya telefon ya "ME"; gateway deposu boş; 14041/14042/14058/14067'de **hiç mesaj
yok**. Yazma politikası doğru çalışıyor (`set_contact_name`: telefon-görünümlü adı gerçek ad her
zaman ezer — `phone_like or NAME_RANK[src] >= current_rank`), yani gerçekten kayıtsız kişiler ve
telefon göstermek WhatsApp Web paritesi. Aynı politika gateway'de `mergeContactName()` ile birebir.

**Yan bulgu:** grup mesajlarında `sender_phone` her satırda **grubun kendi JID'i**
(`905413749073-1589570212@g.us`). `_historyMessageToRecord` (`session-manager.js:1622`) katılımcı
LID olduğunda `jidToPhone()` null döndüğü için konuşma anahtarına düşüyor. Görünümde etkisiz
(`extractCleanPhone` `@g.us` reddeder) ama yanıltıcı bir alan. `participant_jid` gateway'de var ama
**DB'ye yazılmıyor** (Message modelinde kolon yok).

**Doğrulama aracı:** `GET /sessions/:sessionId/contacts` sadece oturum varlığı ister ama
`requireGatewayToken` ile korunur → `X-Gateway-Secret` gerekir. Secret'ı dışarı çıkarmamak için
sorguyu **gateway konteynerinin içinde** `node` ile çalıştır (`process.env.WHATSAPP_GATEWAY_SECRET`).
`docker exec ... sh -c "curl ..."` çalışmaz — konteynerde `curl` yok.


## İsim düzeltmesi — DEPLOY EDİLDİ ve canlı doğrulandı (2026-09-26 00:47–01:02)

Kullanıcı seçimi: **"Depoyu kalıcılaştır + eski satırları onar"** (gateway restart + veri onarımı
yetkisi) ve **"Bırak, doğru davranış"** (telefon başlıklı 8 sohbet değişmeyecek).

**Commit'ler (origin/main):** `55f994e` (disk cache) → `1030850` (DB hidrasyonu) → `4bd7791`
(canlı telefon damgası).

### Prod'a kısmi yama (ddbfbf4'ü sürüklemeden)
- `359fe6d` üzerine cherry-pick: `55f994e`, `1030850` → `d8b9add`; sonra `4bd7791` → `5231b5a`.
- Yama = `git diff d8b9add 5231b5a` → **3 dosya, +51/−2**. `socket-events.js` **dahil değil**.
- Neden: `ddbfbf4` `socket-events.js`'i (+105) ve `session-manager.js`'i (+299) yeniden yazdı ama
  **`socket-connector.js`'e dokunmadı**. Prod'un `socket-events.js`'inde connect bloğunda
  `selfJid/selfPhone` **yok** (telefon yalnızca connector'da öğrenilir) → oradaki yedek damga prod'un
  kod şekline uymuyor, çatışma `git checkout --ours` ile prod tarafına çözüldü. Damganın asıl yeri
  zaten `socket-connector.js:157`.
- Prod'a uygulama öncesi sha256'lar `1030850` ile **birebir** eşleşti (contact-cache `f6d5adbd`,
  socket-connector `a65c9a72`, test `0f960289`); sonrası `4bd7791` ile (sırasıyla `48000647`,
  `d0849b32`, `d3930c05`). Yedek: `/opt/tezlify/backups/stampfix-20260926/`.

### Bulunan ve kapatılan kendi hatam
`setSessionPhone()` yazılmış ama **hiç çağrılmıyordu** → `phoneStamp` her zaman `null` kalıyordu ve
`load()` içindeki telefon-uyuşmazlık koruması (`cachedPhone && currentPhone` iki taraf da dolu
olmalı) **hiçbir zaman ateşlenemiyordu**. İşe yaramayan koruma, koruma yokluğundan kötüdür: var
sanılır. Düzeltme: `lastStore` saklanır, damga değişince kayıt yeniden zamanlanır, `clear()` damgayı
sıfırlar; `socket-connector.js` (creds'ten telefon) ve `socket-events.js` (bağlanmada, idempotent
yedek) damgalar. Test bölümü J canlı akışı kurar (önce phonesuz store, sonra damga) ve artık
farklı numaraya yeniden bağlanınca adların atıldığını doğrular. **31 → 37 assertion.**
Falsifikasyon: eski atıl gövde geri konunca J bölümü `false !== true` ile düşüyor (exit 1).

### Canlı doğrulama (üretim, iki restart)
1. **00:51 restart (cache yok):** `Hydrated contact names from backend database, written=197`.
   Önceden hesaplanan tahmin **197** (677 kişiden, sahibi `f65642ab-4ae5-4d69-945c-8f30c8454bac`) —
   birebir tuttu. `contacts-cache.json` oluştu, 197 kayıt, `session_phone=null` (beklenen: telefon
   henüz bilinmiyor).
2. **01:01 restart (cache var, `4bd7791`):** `Restored contact names from disk cache, restored=197`;
   DB hidrasyonu `written=0` (aynı `history` rank'ında zaten var → `continue`). **İki yol da çalışıyor
   ve doğru sırayla birleşiyor: cache hızlı yol, DB yedek yol.**
3. `session_phone="+905413749073"` — damga artık canlı (önceden `null`).
4. `905076382749@s.whatsapp.net -> "Cevat Aydın" [history]` — 00:12 restart'ında telefona düşen
   **tam olarak o JID**. `_resolveDisplayName()` depoyu `name_source` filtresi olmadan okur
   (`session-manager.js:1608-1613`), dolayısıyla sonraki mesaj adla etiketlenir.

### Yanlış alarm (kayda değer)
`GET /sessions/{sid}/contacts` **restart sonrası da 0 döndü** ve bu ilk bakışta "hidrasyon
çalışmadı" gibi göründü. Sebep: `listContacts()` yalnızca `name_source in ('addressbook','verified')`
döndürür (`session-manager.js:536-541`); hidrasyon `history` rank'ına yazar. Yani 0, endpoint'in
kendi filtresidir, arıza değil. Deponun içeriğini kanıtlamanın doğru yolu **`contacts-cache.json`**
(cache depoyu aynalar) veya hidrasyon log satırıdır — `/contacts` sayısı değil.

### Prod son durum
- Backend `00:47:53`'te yeniden oluşturuldu, migration:
  `backfill_phone_sender_names: 41 mesaj gönderen adı telefondan kişi adına çevrildi.` (tahmin 41).
  Onarım sonrası yüklem **0 satır**; grup 14010 etiketleri `Cevat Aydın 32 / Tolga Cebeci 15 /
  Lazury 1`.
- Gateway `01:01:07`'de yeni imajla (`48000647`/`d0849b32` doğrulandı) yeniden oluşturuldu;
  `POST /sessions/restore` → `{discovered:1, restored:1}`; `CONNECTING → CONNECTED` 2.2 sn.
  DB: `CONNECTED | +905413749073 | t | 01:01:28`. Backend UI'ya 200 dönüyor.
- `/opt/tezlify` hâlâ `359fe6d`'de kirli bir checkout; yama çalışma ağacında commit'siz duruyor.
  `socket-events.js`'teki yedek damga satırı prod'a **bilerek** gitmedi (orada o blok yok).

### Yeniden kullanılabilir ders
- Çakışmayı çözerken **hangi tarafın prod'un şekli olduğunu** diff ile kanıtla: `ddbfbf4` hangi
  dosyalara dokundu, hangisine dokunmadı (`git show --stat ddbfbf4 -- <path>`). Dokunmadığı dosyada
  yama temiz uygulanır; dokunduğunda cherry-pick + `--ours` gerekir.
- Bir koruma yazdıysan **çağrıldığını** doğrula (`grep -rn setSessionPhone src/`). Export edilmiş ama
  çağrılmamış fonksiyon = ölü koruma.

### Kapanış tutarlılık kontrolü (01:05 sonrası)
- **Depo ile DB birebir:** sahibin gerçek adlı kişi sayısı = **197**; hidrasyonun yazdığı sayı da **197**.
  Yani store, DB'nin gerçek-adlı kişilerinin eksiksiz aynası — kaçan satır yok, sürüklenme yok.
- **Kalan 8 telefon etiketi doğrulandı:** her biri için `contacts` satırı **var** ama
  `display_name` telefonun kendisi (`has_real_name = f`). Yani gösterilecek bir ad hiçbir yerde yok;
  kullanıcının "Bırak, doğru davranış" kararı veriyle teyit edildi. (8'i: `+15556599459`,
  `+902123310153`, `+905322334968`, `+905447622185`, `+905459137484`, `+905528073007`,
  `+905543331422`, `+905549411170`.)
- **Yeni mesaj gözlemi yapılamadı:** restart'tan (01:01) sonra gelen kutusuna **hiç mesaj düşmedi**
  (0 satır) — grup sessiz. Bu yüzden "yeni bir mesaj adla etiketlendi" iddiası **canlı gözlemle
  değil**, deterministik zincirle kanıtlandı: depoda anahtar var (`905076382749@s.whatsapp.net` →
  "Cevat Aydın") + `_resolveDisplayName()` depoyu `name_source` filtresi olmadan okuyor. Bir sonraki
  gelen grup mesajı bunu doğrulayacak; iddiayı şimdiden "gözlemlendi" diye sunma.
- Not: `contacts` tablosunda **`name_source` kolonu yok** (yalnızca `phone_e164`, `display_name`,
  `lead_id`, `custom_attributes`). Kaynak/rank bilgisi yalnızca gateway deposunda ve
  `mergeContactName()` içinde yaşar; DB'den sorgularken `name_source` seçmeye çalışma.

## i18n eksik çeviri anahtarları — 19 anahtar (commit `8177a2b`)

Kullanıcı konsolda `[i18n] Missing translation for key: "whatsapp.warmUpDayLabel" (language: "tr")`
hatasını bildirdi. Tek anahtar sanılıyordu; **19 anahtar** çıktı.

**Mekanizma:** `I18nContext.tsx` önce aktif dile, sonra İngilizce'ye bakar; ikisinde de yoksa
`console.warn` basıp **ham anahtar yolunu döndürür**. Yani bu sadece konsol gürültüsü değil —
UI'da `whatsapp.warmUpDayLabel:` gibi literal metin görünüyor.

**Neden mevcut kontrol yakalamadı:** `verify-whatsapp-logic.mjs`'te "en ve tr aynı anahtar kümesine
sahip" kontrolü var ama bu **en'i tr ile karşılaştırıyor**. Her iki sözlükten birden eksik olan bir
anahtar bu kontrolü **geçer**. `syncStage` kontrolü ise elle yazılmış bir allowlist — sadece birinin
hatırladığı anahtarları kapsıyor. Falsifikasyonla kanıtlandı: `warmUpDayLabel`'i iki dosyadan da
silince parity kontrolü yeşil kalıyor (`en=1365 tr=1365`) ama yeni kontrol düşüyor.

**Tarama yöntemi:** `src/` altındaki tüm `.ts/.tsx` içinde `t('...')` / `t("...")` çağrılarını
regex'le çıkar, `en`/`tr` düzleştirilmiş anahtar kümeleriyle karşılaştır. 1487 çağrı, 136 dosya.
`t('a' + b)` biçiminde birleştirme **yok** (hepsi `t('key', {params})`), o yüzden regex güvenli;
tek dinamik site `` t(`whatsapp.pairingCodeStep${n}`) `` ve `[1,2,3,4].map` ile sınırlı.

**Çözümler — içerik kopyalamaktan kaçınıldı (5 yeniden adlandırma, 1 taşıma, 1 yeniden kullanım, 13 yeni):**
- Yeniden adlandırılan **kullanılmayan yetim** anahtarlar: `campaignGroups.pageTitle`→`title`,
  `noGroupsTitle`→`emptyTitle`, `noGroupsDesc`→`emptyDesc`, `common.passive`→`inactive`,
  `blacklist.leadNotSelectedError`→`selectLeadWarning`.
- Taşınan: `leadFinder.stream.saveGroupNoLeads` → düz `leadFinder.saveGroupNoLeads`
  (UI toast'ı, stream satırı değil; `stream.*` emojili canlı akış mesajları için).
- Yeniden kullanılan: `BlacklistAddModal` artık canlı `blacklist.noPhoneInLead`'i kullanıyor
  (kopya anahtar eklenmedi).
- Yeni 13: `leads.deleteSuccess|bulkDeleteSuccess|bulkDeleteAllSuccess`,
  `blacklist.bulkBlacklistSuccess`, `campaignGroups.allGroupsTab|createGroupTab|modalSubtitle`,
  `campaigns.nameRequiredWarning|templateRequiredWarning`, `whatsapp.warmUpDayLabel|connectionFailed|qrInvalidFormat`,
  `leadFinder.saveGroupNoLeads`.
- Toplam: 1354 → **1366** anahtar (13 yeni − 1 taşınan).

**Bonus bulgu:** `CampaignCreateWizard`'da `t('campaigns.nameRequiredWarning') || 'Lütfen …'` gibi
**ölü fallback**'ler vardı: `t()` eksik anahtarda truthy bir string (anahtar yolunun kendisi)
döndürdüğü için `||` hiçbir zaman ateşlenemez. Aynı desen tüm repoda **~50 yerde** var
(`CampaignsPage`, `SpinnerPreviewCard`, `LeadDetailDrawer`, `LocationMultiSelect`, …) — bunlar
sessizce ölü ve AGENTS.md'nin "hardcoded string yasak" kuralını ihlal ediyor. **Ayrı bir temizlik
işi olarak bırakıldı**, sadece düzelttiğim 2 satır temizlendi.

**Doğrulama:** `verify:logic` 45/45 PASS (yeni 2 kontrol dahil), `tsc --noEmit` temiz,
`npm run build` exit 0, üretilen bundle'da 12 örnek dizenin (6 en + 6 tr) tamamı mevcut.

**Önceden var olan başarısızlık (benim değişikliğimden değil):** `verify:pairing` 2 kontrol düşüyor
(`the modal must poll for the QR (calls=0)` ve `Cancel on an errored pairing must release the
socket (cancel=0)`). Değişikliklerimi `git stash` ile alıp tekrar koştum: **aynı 2 hata** çıkıyor
(FAILED (2 checks), aynı assertion'lar). Harness kaynaklı (`calls=0` → fetch stub çağrılmıyor),
ürün hatası değil. Ayrı iş olarak ele alınmalı.

## i18n düzeltmesi — DEPLOY EDİLDİ ve canlı doğrulandı (2026-09-26 08:31–08:36 UTC)

Kullanıcı onayı: **"Deploy now"**. Frontend-only deploy; backend/gateway/db'ye dokunulmadı.

**Push:** `99c275e..f5ebcd8` → `origin/main` (`8177a2b` i18n + `f5ebcd8` memory).

**Release:** `v20260926_i18n_missing_keys`; `frontend_candidate` atomik olarak takas edildi
(`ln -sfn … .new` + `mv -T …`), ardından
`docker compose -f docker-compose.prod.yml up -d --force-recreate --no-deps caddy`.
Zorunlu: Caddy bind-mount'u symlink'i **container oluşturulurken** çözüyor; `up -d` tek başına no-op.

**Bütünlük (uçtan uca):** local dist'in 3 dosyası ile host'taki release'in sha256'ları birebir aynı.
`index-y-X3DlGp.js` = `52cead0101b6be0ad9ce27a3b5576265a115831a011262eae97b718cc608410c`.

**Dört adımlı kabul kanıtı:**
1. `readlink -f` → `/opt/tezlify/releases/v20260926_i18n_missing_keys` ✓
2. container'ın gördüğü: `index-y-X3DlGp.js` ✓ (bind-mount tuzağı yok)
3. Caddy'nin döndürdüğü `index.html`'in referansı: `assets/index-y-X3DlGp.js` (http=200, 1190 B) ✓
4. **Proxy'den çekilen asset'in hash'i** = local build hash'i (http=200, 885125 B) ✓

**Kapsam kanıtı (StartedAt):** yalnız `tezlify-caddy` değişti (`08:34:15`). `tezlify-backend`
`00:47:53`, `tezlify-gateway` `01:01:07`, `tezlify-db` `09-15` **değişmedi** → WhatsApp socket'i
hayatta kaldı, hiçbir backend/gateway restart olmadı.

**Cache politikası:** entry document `no-cache, no-store, must-revalidate`; hashed asset
`public, max-age=31536000, immutable`. Düz reload yeni bundle'ı alır → hard-refresh gerekmez.

**Servis edilen bundle'da daha önce eksik olan dizeler:** `Warm-Up Day`, `Isınma Günü`, `Tüm Gruplar`,
`Grup Oluştur`, `Lütfen kampanya adını girin.`, `WhatsApp bağlantısı kurulamadı`, `QR kodu okunamadı`
— hepsi mevcut. Eski `index-3UEtgr4K` artık hiçbir yerde referans edilmiyor.

**Public URL bulgusu (kayda değer):** `api.tezlify.com` **public DNS'te çözülmüyor**
(`curl` exit 6). Çalışan public adres `https://130.162.247.20.sslip.io` — oradan da http=200 ve
asset hash'i birebir aynı (tls=0). API proxy ardından sağlıklı: `/health` 200,
`/openapi.json` 200, `/api/v1/whatsapp/sessions` 401 (fail-closed, doğru). Backend + gateway
container'ları `healthy`.

**Rollback noktası:** `v20260926_phase6_8_chatthread_singleton` (`index-3UEtgr4K.js`) duruyor.

**Kural olarak kalan:** i18n sınıfı hatanın regresyon kontrolü `verify:logic` içinde
(`every static t(...) call site resolves in both dictionaries`, 1487 çağrı yeri / 136 dosya) —
falsifikasyonla kanıtlandı, `warmUpDayLabel` iki sözlükten de silinince parity kontrolü yeşil
kalırken bu kontrol kırmızı veriyor.

## Sohbet sıralaması — kök neden: `lastMessageRecvTimestamp` (2026-09-26 09:1x–09:2x)

Kullanıcı WhatsApp Web sohbet listesini (~110 satır) yapıştırıp Tezlify'daki sıralamanın
aynısı olmadığını bildirdi. Varsayım yapmadan araştırıldı.

**Boru hattı tutarlı:** gateway (`session-manager.listConversations`), backend
(`list_conversations`, `ORDER BY last_message_at DESC NULLS LAST, id DESC`) ve frontend
(`compareConversationsByActivityDesc`) ÜÇÜ de `last_message_at`'e göre sıralıyor. Yani
sıralama mantığı değil, DEĞER yanlış.

**Kök neden (kanıtlı):** `socket-events.js` içindeki `messaging-history.set` işleyicisi
sohbetin sıralama damgasını `chat.lastMessageRecvTimestamp`'ten türetiyordu. Baileys'in kendi
tip tanımı bu alanı "**the last message received from the other party**" diye tanımlıyor;
WhatsApp Web ise sohbet listesini sohbetteki **MUTLAK son mesaja** (`conversationTimestamp`)
göre sıralıyor. Sonuç: **en son mesajı BİZİM attığımız her sohbet, karşıdan en son ALINAN
mesajın zamanına göre sıralanıyor** → WhatsApp Web'in koyduğu yerin altına düşüyor.

Canlı üretim ölçümü (113 sohbet): **24 sohbetin damgası kendi en yeni mesajından ESKİ**, ve bu
24'ün **23'ünde en yeni mesaj OUTBOUND**. En kötüleri: Ufuk Biber **36 gün**, Annem 20 gün,
Saim Başkan 16 gün. Ayrıca 2 sohbetin damgası hiç yoktu (`lastMessageRecvTimestamp` yoktu,
`newest` da yoktu) → listenin dibine düşüyorlardı.

**Düzeltme (`4fc791a`, gateway-only):** öncelik artık
`conversationTimestamp` → `lastMsgTimestamp` → en yeni yerel mesaj (gönderilen VEYA alınan) →
`lastMessageRecvTimestamp` (artık SON sırada, eskiden İLK'ti).
- `toPositiveSeconds`: protobuf uint64 (number|string|Long) çevirir — `Number(Long)` NaN'dır ve
  geçerli bir damgayı sessizce düşürürdü. Ayrıca milisaniye ölçeğini saniyeye indirir: backend
  `last_message_at`'i YALNIZCA İLERİ taşıdığı için uzak-gelecek bir damga listeyi kalıcı olarak
  zirveye çiviler ve hiçbir senkron düzeltemez.
- `messaging-history.set` artık `chats.update`/`_touchChat` ile aynı monotonluk korumasına sahip
  (gün içinde 28 ingest → geç gelen chunk sohbeti geriye çekmemeli).
- Test: `scripts/test-chat-activity-order.mjs` (9 kontrol), `npm test` zincirine eklendi.
  **Falsifiye edildi:** eski öncelik geri konunca test tam değer üzerinde düşüyor
  (received-only 2026-07-31 dönüyor, mutlak 2026-09-05 yerine).

**Deploy (09:21 UTC):** `git apply` ile 4 dosyalık delta yama (prod `359fe6d` olduğu için
`socket-events.js` hunk'ları −56 offset ile oturdu; `ddbfbf4` sürüklenmedi). İmaj kuruldu ve
imaj İÇİNDE doğrulandı: her iki dosyanın sha256'sı host ile birebir, eski hatalı satır 0,
`node --check` OK. `--force-recreate --no-deps gateway`; hat `POST /sessions/restore` ile
QR'sız geri bağlandı (`restored:1`, CONNECTED, +905413749073). Kapsam kanıtı: yalnız gateway
StartedAt değişti; backend/caddy/db aynı. DB sağlam: **113 sohbet / 529 mesaj / 1514 kişi**.

**DÜRÜST SINIR (önemli):** gateway'in sohbet listesi **memory-only** ve yeniden bağlanmada
YENİDEN KURULMUYOR. Log: `First connection, awaiting history sync notification with a 20s
timeout` → `History sync finalized`, ama **0 ingest**. Restart sonrası gateway'de yalnız
**12/113** sohbet var ve mesaj deposu da boş. Yani düzeltilmiş damgalar, WhatsApp yeniden
geçmiş gönderene kadar (uygulama/telefon aktivitesiyle tetikleniyor; restart tetiklemiyor)
AKMAYACAK. Bu yüzden düzeltmenin canlı etkisi bugün gözlemlenemedi; doğrulama birim testi +
deterministik argümanla sınırlı.

**Kalan iş (önerilen):** gateway sohbet listesini diske kalıcılaştır (`contacts-cache.json`
deseninin aynısı). Aksi halde her restart sıralama verisini sıfırlıyor. Ayrıca backend'in
sohbet-listesi senkronu (`sync.py:888`) `last_message_at` güncellemesini boş olmayan bir
preview'a bağlıyor; `conversation_updated` olay yolu (`events.py:1123`) ise koşulsuz uyguluyor —
bu asimetri, yerel mesajı olmayan bir sohbetin düzeltilmiş damgayı kaçırmasına yol açabilir.

**Ayrıca bulunanlar (kapsam dışı bırakıldı, kayda değer):**
- 12 grupta **çift contact satırı** var (id 538-582 ve 9869-9880, aynı `jid:...@g.us`).
- WA Web'de olup Tezlify'da hiç olmayan 5 sohbet: WhatsApp Business, Facebook, Trendyol,
  OpenAI, @kxntrix — `contacts`'ta hiç yoklar (ingest boşluğu).
- Kendi kendine sohbet ("Hat 1", conv 14345) bugün 08:46–08:52 arası **~12 adet BOŞ gövdeli
  OUTBOUND TEXT** mesaj almış (toplam 13 boş OUTBOUND TEXT'in 12'si). Bu, sohbeti Tezlify'da
  #2'ye çiviyor; WhatsApp Web aynı sohbeti #12'de gösteriyor → ayrıca incelenmeli.
- Kullanıcının yapıştırdığı liste, mesajdan-türetilmiş damgalarla **1–41 arası kesinlikle
  monoton**; sapmalar tam olarak damgası gateway'den gelen (yerel mesajı olmayan) sohbetlerde
  toplanıyor. Yani DB'nin mesajdan-türetilen değerleri doğru, gateway'den gelenler şüpheli.

## Hafıza katmanlandırması + deploy skill güncellemesi (12:31)

- `MEMORY.md` enjeksiyon limitini aştığı için **kırpılıyordu** (yani enjekte edilen kopya sonunu
  sessizce kaybediyordu). Katmanlandırıldı: `MEMORY.md` kural özetleri + operasyonel gerçekler
  olarak kaldı (12.484 → **9.910 B**), ayrıntı/kanıt `reference/whatsapp-subsystem-invariants.md`
  içine taşındı (A4–A10: mutlak-son-mesaj sıralaması, önbellek-öncelik zinciri, monoton kural,
  memory-only store'lar, gönderici etiketleri, contacts endpoint tuzağı, DB damga kökeni ve
  113 sohbetlik canlı ölçüm).
- Tuzak: **aynı dosyaya paralel `Edit` çağrıları birbirini eziyor** — üç eşzamanlı düzenlemeden
  yalnızca ilki diske indi (dosya +62 B büyüdü, yani ikisi kayboldu). Aynı dosyada sıralı düzenle
  veya tek `Write` ile baştan yaz.
- `git-checkout-prod-partial-service-deploy` skill'ine 6 yeni tuzak eklendi (208 → 232 satır):
  `git diff --stat <base>..HEAD` kapsamı **şişirir** (atlanan commit'ler listeye girer);
  `git apply --check` offset'i teşhis bilgisidir; `--force-recreate` sonrası container `/tmp`'sini
  kaybeder → `docker cp` tekrarlanmalı; **yeniden başlatma history sync'i tetiklemez**, yani
  etkisi history sync'ten geçen bir düzeltme aynı oturumda canlı doğrulanamaz (bunu düz metin
  söyle, doğrulanmış gibi ima etme); iç içe `sh -c "… node -e \"…\""` tırnaklaması çalışmaz →
  `/tmp`'ye yaz + `scp` + `docker cp`; `\pset format unaligned` içinde üst düzey `SELECT` satırı
  **sütun başına bir kez** basar → CTE ile sar.

---

## 2026-09-26 — Bildirim türü son mesajlı sohbetlerin sıralaması (Fix B + Fix C)

**Kullanıcı isteği:** "bazı gruplarda veya kişilerde görüntülü arama, ifadeler … 'Bu grubun ayarlarını …
değiştirdiniz' gibi uyarılar, en son ki mesajda ses dosyaları, 'Muktedir kişilere eklendi' … 'Barkın
Semruk, @barkinirgen kullanıcı adını oluşturdu' … 'Bu mesajı sildiniz', 'Şu mesaja ifadesini bıraktınız'
… tüm uyarıları göz önünde bulundurarak sıralamayı tekrardan kontrol et ve hataları fixle."

**Kök neden:** `last_message_at` (tek sıralama anahtarı) **önizleme metnine bağlıydı.** Metin taşımayan
bir mesaj (ifade, REVOKE, arama kaydı, grup ayarı uyarısı, "X eklendi", kullanıcı adı duyurusu, ses-only)
boş özet üretir; `should_apply_last_message` `not summary` için `False` döndüğünden **damga da
reddediliyordu** ve sohbet eski liste konumunda donuyordu. Üç yazıcıdan ikisi bu kapıya bağlıydı:
`conversations.py::apply_conversation_last_message` (tek boolean, iki alan) ve `sync.py:887` /
`sync.py:1733` (tüm damga güncellemesi `if gw_summary:` içinde). Üçüncüsü —
`events.py:1123-1125` `conversation_updated` — damgayı **koşulsuz** uygular; kullanıcının "genel olarak
sıralama doğru" demesinin sebebi tam olarak bu asimetridir.

**Canlı kanıt (113 sohbet, gateway `GET /sessions/<id>/conversations` → `{items,total}`):**

```
gateway sohbet             : 113
  boş last_message_preview :  21
  NULL last_message_at     :   5
DB conversations           : 113
  NULL last_message_at     :   5   (aynı beşi)
  boş last_message_preview :  21   (aynı küme)
```

Karar verici kanıt: conv **14458 ("Hat 1")** damgası `09:35:56`, en yeni mesajı `09:44:25` — **8,5 dakika
donmuş** (13 mesajlık outbound patlaması, hepsi `body = NULL`, hepsi `type = TEXT`), oysa gateway
`09:44:25` bildiriyor. `last_message_preview` NULL olduğu için `should_apply_last_message` her seferinde
`False` döndü. Bu damga yalnızca `conversation_updated`'dan gelebilirdi → 09:34:56 snapshot'ı 09:44:25
düzeltmesini kaybetmiş.

**İki ayrı alt-mekanizma ayrıldı:**
- **(a) damga donmuş** — mesaj kaydedilmiş, damga ilerlememiş. "Hat 1" tam olarak bu; düzeltildi.
- **(b) mesaj gateway'den hiç öğrenilmemiş** — 5 NULL damgalı sohbet; hepsi **yalnızca grup konusundan**
  bilinen gruplar (`name_source: "group_subject"`, `created_at == updated_at == 09:34:56`,
  `last_message_preview: ""`). WhatsApp hiç mesaj ve hiç zaman damgası göndermemiş → hiçbir kod yolu
  kurtaramaz (A13). (a)'dan ayrı bir boşluktur; "düzeltildi" diye sunulmadı.

**Yapılan düzeltmeler:**
- **Fix B** — `repositories/conversations.py::apply_conversation_last_message` artık önizleme ve damgayı
  **ayrı** kararlar olarak uygular (damga `ts` kesinlikle daha yeniyse ilerler; `ts is None` damgayı
  ilerletmez → Faz 10 RC-1 "gelecek damga tohumlama" yasağı korunur). `sync.py`'nin **iki** snapshot
  noktası `elif gw_ts is not None and (conv.last_message_at is None or gw_ts > conv.last_message_at)`
  dalını kazandı; damga artık `if gw_summary:` kapısının dışında.
- **Fix C** — `messages/message-classifier.js`'e `systemContentMarker()` eklendi, `summarizeWaMessage` ve
  `session-manager.js::_ingestUpsertMessage` canlı yoluna bağlandı. Metinsiz sistem içeriği artık
  `[CALL]/[REACTION]/[REVOKED]/[SYSTEM]/[POLL]/[EVENT]` **önizleme işareti** taşır; `message_type`
  **genişletilmedi** (`MessageType`'ta `SYSTEM`/`REACTION` yok, migration gerekirdi). `messageStubType > 0`
  (Long olabilir → `toNumber()`) veya `protocolMessage.type === 0` (REVOKE) tetikler; `0`/yok → **işaret
  üretilmez** (uydurma yasağı). Üç paralel etiket tablosu (gateway `whatsapp-formatting.js`, backend
  `preview_normalization.py`, frontend `whatsappPreview.ts`) + `locales/{en,tr}.ts` altı anahtarla hizaya
  getirildi — frontend kopyası zaten altı etiket gerideydi ve jenerik "Mesaj" gösterecekti.

**Falsifikasyon (her düzeltme eski kodla kanıtlandı):**

| Geri alınan dosya | Yeni testin verdiği hata |
| --- | --- |
| `conversations.py` | `AssertionError: assert False is True` |
| `sync.py` | `assert got is not None` |
| `whatsapp-formatting.js` | `actual: 'Mesaj', expected: undefined` |

**Doğrulama:** backend **1169 pass / 4 skip / 0 fail** (şema-only DB; taban 1166 + 3 yeni); gateway
`npm test` exit 0 (`System-content preview verification: PASS (14 checks)`, sipariş testleri 9/9);
frontend `tsc --noEmit` exit 0, `verify:logic` 45/45, `npm run build` exit 0; `node --check` üç gateway
dosyasında OK. Kirli dev DB'de görülen 4 hata `git stash` ile **değişiklikten bağımsız** olduğu kanıtlandı.

**Yan bulgu — hat yeniden bağlandı ve mesaj geçmişi silindi:** `messages` 529 → **44** satıra düştü, 113
sohbet satırının tamamı `09:34:56`'da sıfırdan yazıldı ve aynı saniyede yeni bir `whatsapp_sessions`
satırı (id 86, `+905413749073` — kullanıcının kendi numarası) oluştu. Bu, **09:21 gateway deploy'undan
sonra hattın bilinçli olarak history'siz yeniden bağlanması**, benim değişikliğim değil; ama o zaman
damgasından önce/sonra karşılaştırma yapmayı geçersiz kılıyor. Rapor edildi, "düzeltildi" denmedi.

**Commit edilmedi / deploy edilmedi.** Backend + gateway + frontend değişti → üç servisli deploy; duran
anlaşma gereği **kullanıcı onayı bekliyor**.

**Kontrol edilen, sorun olmayanlar:** `pinned` gateway payload'ında hiç yok (tutarlı, modellenmemiş);
`TYPE_PREVIEW_LABELS`'a eklenen `CALL`/`REACTION`/`SYSTEM` güvenli, çünkü tablo yalnızca (a) tam
köşeli-işaret eşleşmesi ve (b) `message_type` fallback'i ile tüketiliyor ve `MessageType`'ta böyle üye
yok → yanlış etiket üretilemez.

**Bellek bakımı:** `MEMORY.md` enjeksiyon limitini aştığı için yeniden katmanlandı (9.910 → **6.848 B**):
operasyonel topoloji/deploy/doğrulama detayı yeni `reference/production-ops.md`'ye taşındı; yeni
invariant'lar `reference/whatsapp-subsystem-invariants.md`'ye **A11** (boş önizleme damgayı kilitleyemez),
**A12** (işaret protokolü, üç tablo paritesi) ve **A13** (yalnızca-konu grubu → damga yok) olarak eklendi.
`WHATSAPP_BASELINE_CONTRACT.md` de güncellendi (iki yeni INVARIANT bloğu + üç dosya satırı).

---

## 2026-09-26 — QR poll 502'si: kalıcı satırın İKİ yazıcısı (G-PROMOTION-SINGLE-WRITER)

**Kullanıcı raporu:** `GET /api/v1/whatsapp/pairing/6e22e8d6-…/qr` → **502**.

**Gerçek neden (canlı log, varsayım değil).** `_bad_gateway` ham istisnayı WARNING'e yazıyor; prod
logunda bulundu:

```
11:43:56,910 WARNING WhatsApp gateway hatasi: (asyncpg.UniqueViolationError) duplicate key value
            violates unique constraint "ix_whatsapp_sessions_gateway_id"
DETAIL:  Key (gateway_id)=(f157eca0-d57f-43bb-9e67-8a14d03a852e) already exists.
11:43:56,917 INFO [WhatsApp][6.8] Promotion created durable session id=87 gateway=f157eca0-…  ← 7 ms sonra
```

**Kök neden:** Taze bağlanan bir gateway oturumu için kalıcı `whatsapp_sessions` satırını **iki bağımsız
yazıcı** oluşturuyordu: (1) olay güdümlü `promote_ephemeral_pairing` (aynı `connection.open` ile tetiklenir)
ve (2) bu endpoint, ki aynı relink / reuse / INSERT dizisini elle kopyalamıştı. İkisi de "bu gateway id için
satır yok" okuyup sonra yazdı → biri unique index'te kaybetmek zorundaydı. `promotion.py` ırk-toleranslıydı
(rollback + yeniden oku); endpoint'in INSERT'i değildi ve 502 tarayıcıya ulaştı. **Eşleşme aslında
BAŞARILIYDI** — yani kullanıcı sahte bir hata gördü (modal ERROR'a düştü ve polling durdu).

**Neden delegasyon (yamadan daha doğru):**
- `promotion.py`'nin kendi docstring'i bunu zaten söylüyor: 6.8 tam da "promotion tarayıcının polling'ine
  sessizce bağımlıydı" diye yazılmış; `promote_ephemeral_pairing` "tek, idempotent giriş noktası".
- Elle yazılmış dallar prod'da **7 günde SIFIR kez** çalışmış (`Relink Scenario A` / `Reused existing
  session` / `First-time QR pairing` log satırları yok). Tek ürettikleri şey 502 oldu.
- Hedefli-relink dalı **erişilemez**: hiçbir HTTP çağıranı `logical_session_id` set etmiyor
  (`start_pairing_session`'ın tek çağrısı endpoint ve onu geçmiyor).
- Endpoint'in rebind dalı, promotion'ın belgelenmiş invariant'ıyla **çelişiyordu** ("başka bir gateway
  altında CONNECTED olan bir oturum asla üzerine yazılmaz").

**Düzeltme:** `get_pairing_qr` artık yazmıyor; gateway `CONNECTED` dediğinde `promote_ephemeral_pairing`'e
delege ediyor (lazy import — `cancel_pairing_session`'ın zaten kullandığı desen). Ret (None) artık
`PairingPromotionRefused` → **409**, 502 değil: gateway sağlıklıyken "gateway'e ulaşılamadı" demek yalandır
(AGENTS.md §1.1). Kullanılmayan relink import'ları da düştü → `check_circular_dependencies.py` döngü sayısı
**6 → 4**.

**Falsifikasyon:** `sessions.py` stash'lendi, test kaldı → yeni test **üretim hatasının birebir aynısıyla**
düştü: `WhatsApp gateway hatasi: (sqlite3.IntegrityError) UNIQUE constraint failed:
whatsapp_sessions.gateway_id` ve `assert 502 == 200`.

**Yeni testler:** `test_p68_qr_poll_survives_concurrent_promotion` (yarışı kazanan satırı önceden commit
edip poll'un onu **adopt ettiğini** ve kopya üretmediğini doğrular), `test_p68_qr_poll_refusal_is_409_not_502`
(dürüstlük kuralını sabitler). `test_phase154_session_recovery.py::test_no_duplicate_public_session`
güncellendi — `sessions.perform_atomic_relink`'i patch'liyordu, yani **kaldırdığım implementasyonu**
doğruluyordu; niyeti aynı, kablolama yeni (artık promotion'ın relink'ini patch'liyor).

**Doğrulama:** backend **1171 passed / 4 skipped / 0 failed** (şema-only DB; taban 1169 + 2 yeni);
`check_circular_dependencies.py` exit 0 ve tabandan **daha az** döngü. Frontend/gateway değişmedi.

**Not (kullanıcı kararı bekleyen, değiştirilmedi):** "zaten CONNECTED+aktif bir telefonu yeniden eşleştirme"
durumunda eski QR yolu satırı yeni gateway'e **rebind** ediyordu, promotion ise **reddediyor**. Belgelenmiş
invariant reddetmek olduğu için ona uydum; ama reddedilen durumda satır eski (ölü olabilir) gateway'i
göstermeye devam eder ve o oturumun olayları yönlendirilemez. Bir sonraki olayda satır
DISCONNECTED/RELINK_REQUIRED'a düşünce relink yolu kendini onarır. Yine de bu bir ürün kararı — rebind mi,
ret mi — ve kullanıcıya açıkça soruldu.

**Tuzak (kendi belleğimde yazılı olmasına rağmen tekrar düştüm):** bu makinede `grep "a\|b"` (BRE
alternation) **sessizce hiçbir şey** eşleştirmiyor — `grep -E` veya Grep aracı kullanılmalı. Bir aramada
"hiç sonuç yok" sonucunu bu yüzden aldım; dosya aslında eşleşiyordu.

---

## Kullanıcının bildirdiği 5 WhatsApp sorunu — kök neden + düzeltme (2026-09-26, geç oturum)

Kullanıcı tek mesajda 5 sorun bildirdi: (1) grup fotoğrafları QR eşleşmesinde yüklenmiyor, (2) grup içi
gönderen adları kısmen telefon kısmen rehber adı, (3) sohbet açılışında sağ taraf çok geç yükleniyor,
(4) arşivli gruplar "Tümü" sekmesinde görünüyor, (5) "Arşivlenmiş" sekmesinde gerçekten arşivledikleri yok,
sadece 1 sohbet var. Hepsi canlı kanıtla kök-nedenlendi; 4'ü düzeltildi, 5. için kurtarma kararı bekliyor.

### 1. Grup avatarları — DÜZELTİLDİ
`_ensureGroupSubjects` → `seedGroupChat` sohbeti `avatar_url: meta?.imgUrl || null` ile yaratır; yalnızca
konusundan bilinen grubun mesajı olmadığı için `_touchChat` hiç koşmaz ve tek seferlik
`_scheduleBackgroundAvatarFetch` (history-sync %100'ünde kurulur) seed geçişinden **önce** bitmiş olur.
Canlı ölçüm: 112 sohbetin 12 grubundan **9'unda avatar yok**; zorla `POST /sessions/{sid}/avatar/refresh`
9'un **5'ini** kurtardı → fotoğraf sunucuda VAR, istek hiç atılmamış. Düzeltme: `applySubject` sonunda
`if (!chats.get(key)?.avatar_url) void this._ensureChatAvatar(session, key);` (`session-manager.js:1472`).
`_ensureChatAvatar` in-flight korumalı ve jid başına 10 dk throttle'lı → her geçişte çağırmak güvenli.
Guard: `scripts/test-group-avatar-fetch.mjs` (3 kontrol), `npm test`'e bağlandı.
**Falsifikasyon:** `session-manager.js` stash → kontrol A tam da hedeflenen assertion'da düştü.

### 2. Gönderen adları — DÜZELTİLDİ
`core/migrations.py::backfill_phone_sender_names` **yalnızca açılışta** koşar. Taze bir history sync
mesajları VE adları aynı geçişte yazar → onarım tam gerektiği anda çalışmaz. Canlı: 72 grup inbound
mesajının 28'i telefon, 44'ü adlı; `contacts` gerçek adı tutuyor (`+905076382749` → `Cevat Aydın`,
`name_source=addressbook`) ama `messages.sender_name` hâlâ çıplak telefon. Düzeltme:
`SyncOrchestrator._repair_phone_sender_names` (SQL `concat` yerine **Python** — SQLite taşınabilirliği),
`finalizing` fazından çağrılır. Testler: `test_28` (onarım), `test_29` (tenant izolasyonu).

### 3. Açılış gecikmesi — DÜZELTİLDİ
`list_messages` düz-açılış kritik yolunda `_hydrate_or_tolerate_provider_timeout`'u bekliyordu; her
sohbet açılışı gateway provider beklemesini ödüyordu. Canlı log: `elapsed_ms` 3005/3225/4016/4018/4311.
Düzeltme: blok 3 dala ayrıldı — 0 satır (tam bütçe), **yerel satır varken düz açılış (anında dön)**,
`before` cursor (sayfalama, `timeout_ms=None`). `OPEN_PATH_PROVIDER_TIMEOUT_MS` kaldırıldı.

### 4+5. Arşiv — SÖZLEŞME DÜZELTİLDİ, KURTARMA KARARI BEKLİYOR
İki semptom TEK kök nedene iniyor. **Kanıt:** prod DB `111 is_archived=false + 1 true`; gateway'in kendi
canlı store'u da **112 sohbetin 1'ini** arşivli gösteriyor — ve `has_archived_key=112` (hepsi `archived`
anahtarı taşıyor, 111'i `false`).

**Kök neden, iki katman:**
1. **Baileys conditional yutması (upstream, yalnızca initial sync):** `chats.js:998`
   `resyncAppState(ALL_WA_PATCH_NAMES, true)` → `processSyncAction` arşiv mutation'ını
   `conditional: getChatUpdateConditional(id, msgRange)` ile yayar. `chat-utils.js:856`'daki koşul
   **yalnızca `isInitialSync` iken** döner ve sohbet `historySets.chats`/`chatUpserts` içinde YOKSA
   `undefined` döner. `event-buffer.js` `append()` (`conditionMatches === undefined`) güncellemeyi
   `chatUpdates`'a koyar; `flush()` onu `newData`'ya taşır ve **asla yaymaz** — sonsuza dek taşınır.
   Mesaj geçmişi gelmeyen (büyük/sıfır mesajlı) grupların arşiv durumu gateway'e hiç ulaşmaz.
   **`Utils/history.js` içinde `archived` eşlemesi YOK** → arşiv durumu history sync'ten ASLA gelemez,
   yalnızca app-state'ten. Canlı: 72 saatte `resyncing regular from v0` **6 kez** (her biri full snapshot,
   `isInitialSync=true`).
2. **Gateway bilinmeyeni `false` olarak iddia ediyordu (bizim hata, sözleşme ihlali):** dört yazıcı da
   `?? false` / sabit `false` kullanıyordu (`socket-events.js:575,695`, `session-manager.js:1305,1489`).
   Backend sözleşmesi ise **"anahtar yoksa dokunma"** (`events.py:1104`, `sync.py:985,1847` —
   `if "archived" in payload:`) — yani backend bu düzeltme için zaten yazılmıştı, ama anahtar her zaman
   gönderildiği için **koruma ölüydü** (bkz. "exported-but-never-called guard" anti-deseni).
   Ayrıca gateway store'u yalnızca bellekte yaşar → **restart sonrası** gelen history chunk'ı DB'de `true`
   olan arşivi `false` ile eziyordu.

**Düzeltme:** `utils/whatsapp-formatting.js`'e `archivedPatch(known)` eklendi — bilinmiyorsa `{}` döner,
böylece `JSON.stringify` anahtarı düşürür ve backend'in mevcut koruması ateşlenir; biliniyorsa
`{archived: Boolean(known)}` (unarchive de yayılır). Dört yazıcı da buna bağlandı.
Guard: `scripts/test-archive-state-contract.mjs` (8 kontrol; A–D saf sözleşme, E–G **gerçek `_touchChat`**
ile restart-clobber regresyonu, H yapısal: kaynakta `archived ... ?? false` kalmasın). `npm test`'e bağlandı.
**Falsifikasyon:** yalnızca iki yazıcı dosyası stash'lendi (helper bırakıldı) → E tam da
"an unknown archive state must NOT be written as a definite false" assertion'ında düştü.

**Dürüst sınır:** bu düzeltme **gelecekteki kaybı** durdurur; mevcut 111 sohbetin arşiv durumunu
**geri getirmez** (gateway onları hiç öğrenmedi). Kurtarma, app-state'in yeniden teslimini gerektirir:
`regular` collection'ının `app-state-sync-version`'ını sıfırlayıp `resyncAppState(ALL_WA_PATCH_NAMES, false)`
çağırmak (version 0 → sunucu full snapshot; `isInitialSync=false` → koşul `undefined` → arşiv
güncellemeleri koşulsuz yayılır). **Yan etki:** TÜM app-state mutation'ları yeniden ateşlenir; okunmuş
sohbetler `unreadCount: 0` yayar → okunmamış rozetleri temizlenebilir. Bu yüzden **ürün kararı** olarak
kullanıcıya bırakıldı (alternatif: düşük riskli, kullanıcı etkilenen sohbetleri yeniden arşivler — bu yol
artık kalıcıdır çünkü non-initial-sync mutation'lar koşulsuzdur).

**Doğrulama:** gateway suite tamamen yeşil (iki yeni guard dahil); backend **1 failed / 1172 passed /
4 skipped** — tek hata **önceden var olan** `test_ingest_message_new_degenerate_no_conv` (stash ile
tabanda da düştüğü doğrulandı).

**Ortam tuzağı (yeni):** prod SSH kullanıcısı **`ubuntu@130.162.247.20`**, anahtar `~/.ssh/id_tezlify_oracle`
— `root@` "Permission denied (publickey)" verir. Ayrıca iç içe tırnaklı `ssh "... node -e \"...\""` bozuluyor
ve `docker exec -i ... node /dev/stdin` pipe'ta `ENOENT` veriyor; çözüm: betiği base64'leyip
`docker cp` ile konteynere atıp orada çalıştırmak.


